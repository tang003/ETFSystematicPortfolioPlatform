# 09 开发日志

## v0.64.0-internal-strategy-engine-registry - 2026-07-14

阶段：策略模块内部插件化

### 完成内容

- 新增内部策略引擎目录 `backend/app/strategies/`。
- 新增策略引擎协议和上下文：
  - `backend/app/strategies/base.py`
- 新增策略引擎注册表：
  - `backend/app/strategies/registry.py`
- 将当前正式策略算法拆分为独立引擎：
  - `backend/app/strategies/factor_rotation.py`
- 改造 `backend/app/services/strategy_service.py`：
  - 继续负责策略配置、内置策略补齐、运行记录、数据加载和落库。
  - 通过 `strategy_config.config.engine` 查找注册引擎。
  - 保留 `construct_target_weights`、`construct_target_weights_with_tradability`、`score_confidence` 等兼容导出，避免回测和旧测试断裂。
- 增加策略注册制测试：
  - 校验 `factor_rotation` 已注册。
  - 校验未知策略引擎会被拒绝。
- 更新策略设计和策略/数据源管理文档。

### 设计说明

- 本次采用“内部插件化”，不支持用户上传 Python 策略文件，不做运行时动态加载第三方代码。
- 这样可以先解决策略算法集中在 `strategy_service.py` 的臃肿问题，同时避免服务器代码执行安全风险。
- 后续新增算法型策略时，应在 `backend/app/strategies/` 下新增独立引擎，并在 registry 中注册。
- 回测模块当前仍通过兼容 helper 复用目标权重构造逻辑，后续应进一步改为调用统一策略引擎。

### 验收要点

- 现有策略 API 和前端调用方式不变。
- 现有内置策略仍使用 `factor_rotation` 引擎。
- 策略 helper 单元测试通过。
- 后端语法编译通过。

## v0.63.3-agents-review-hardening - 2026-07-14

阶段：AGENTS 开发规范审查和量化约束补强

### 完成内容

- 对照当前代码、README 和 docs 核心设计文档重新审查 `AGENTS.md`。
- 将 `AGENTS.md` 从通用开发说明升级为更严格的量化系统开发规范。
- 新增“当前审查结论”，明确文档冲突时的优先级：
  - 当前代码。
  - 最新开发日志和 API 文档。
  - 专题设计文档。
  - README 中早期描述。
- 补强正式数据源口径：
  - 当前正式 ETF 行情、交易日历、ETF 档案和主资料以 Tushare 为准。
  - AKShare / Eastmoney 仍可能存在于旧文档和历史代码中，但不得未经评审重新进入正式清洗行情。
- 补强数据流规范：
  - ETF 基础池、原始行情、清洗行情、因子、策略、目标组合、持仓、风控、回测、报告和 AI 投研的标准流转。
  - 策略、因子、回测、ETF 详情和 AI 投研默认读取 `market_data_clean`。
- 补强数据库规范：
  - 表结构必须通过 Alembic。
  - model、schema、service、API 文档、前端类型和数据库文档必须一致。
  - JSON 字段和可查询字段的拆分规则。
- 补强策略、风控和回测规范：
  - 策略结果必须追溯到 `run_id`、`strategy_code`、`strategy_version` 和 `run_date`。
  - 风控调整必须落库并保留调整前后数值。
  - 回测不得使用未来函数，必须使用本地清洗行情。
- 补强 Docker 部署规范：
  - 生产部署优先使用 `scripts/deploy_production.py`。
  - 外部 Caddy 模式下不得随意占用 80/443。

### 设计说明

- 本次只优化开发协作规范，不修改业务代码。
- 重点解决旧文档中 AKShare / Eastmoney 描述与当前 Tushare-only 正式模式之间的冲突。
- 后续 AI 接手项目时，应优先按新版 `AGENTS.md` 执行开发、测试、文档和部署流程。

### 验收要点

- `AGENTS.md` 已包含数据流、数据库、API、策略、风控、回测、Docker、Git 和密钥安全约束。
- 开发日志已记录本次规范审查。

## v0.63.2-chinese-agents-guide - 2026-07-14

阶段：中文化 Codex / AI 开发协作规范

### 完成内容

- 将根目录 `AGENTS.md` 从英文版改为中文版。
- 保留原有结构和约束内容：
  - 项目背景。
  - 当前能力。
  - 技术栈。
  - 目录结构。
  - 数据处理规范。
  - 数据库设计规范。
  - API 开发规范。
  - 策略开发规范。
  - 回测规范。
  - 测试要求。
  - 文档同步规则。
  - Git 和部署规范。
  - 密钥安全规范。
  - 禁止事项。
  - 待确认边界。

### 设计说明

- 中文版更适合用户自己阅读，也更适合后续把项目交给中文语境下的 AI 快速接手。
- 未确认能力继续保留“待确认”标记，避免编造不存在的技术能力。

### 验收要点

- `AGENTS.md` 已为中文内容。
- 本次只修改文档，不涉及前后端运行逻辑。

## v0.63.1-agents-development-guide - 2026-07-14

阶段：新增 Codex / AI 开发协作规范

### 完成内容

- 新增根目录 `AGENTS.md`。
- `AGENTS.md` 包含：
  - 项目背景和当前开发目标。
  - 已确认技术栈。
  - 目录结构说明。
  - 后端、前端、数据处理、数据库、API、策略、回测开发规范。
  - 测试和验证要求。
  - 文档同步规则。
  - Git 和部署规范。
  - 密钥安全要求。
  - 禁止事项。
  - 待确认边界。
- 更新 `docs/00_文档导航.md`，将 `AGENTS.md` 纳入推荐阅读顺序和 AI 最小上下文。

### 设计说明

- `AGENTS.md` 只记录当前代码和文档中能确认的技术事实。
- 未确认能力使用“待确认”标记，避免为填充内容编造不存在的技术。
- 该文件用于后续 Codex 或其他 AI 快速理解项目，并约束修改、测试、文档、Git 和密钥处理行为。

### 验收要点

- 根目录存在 `AGENTS.md`。
- 文档导航已加入 `AGENTS.md`。
- 本次为文档变更，不涉及前后端运行逻辑。

## v0.63.0-juhe-finance-news - 2026-07-14

阶段：接入聚合数据财经新闻源

### 完成内容

- 新增新闻资讯数据库表 `news_article`。
- 新增迁移 `20260714_0008_news_article.py`。
- 在 `data_source_config` 中登记 `juhe_finance_news` 数据源：
  - 类型：`news`
  - 默认接口：`https://apis.juhe.cn/fapigx/caijing/query`
  - 用途：`news`
  - 业务适配状态：`runtime_supported`
- 新增后端新闻服务：
  - 从数据源管理读取聚合数据 Key。
  - 调用聚合财经新闻接口。
  - 按 `source + external_id` 去重入库。
  - 基于标题关键词、ETF 代码、ETF 名称和跟踪指数做初步关联。
- 新增新闻 API：
  - `POST /api/news/sync`
  - `GET /api/news`
  - `GET /api/news/related/{symbol}`
- 新增前端“新闻资讯”页面：
  - 手动同步最新新闻。
  - 新闻列表。
  - 关键词搜索。
  - ETF 代码过滤。
  - 展示关联 ETF、关键词、资产类别和影响等级。
- 左侧菜单新增“新闻资讯”入口。
- 更新文档：
  - `docs/03_数据库设计.md`
  - `docs/04_API接口文档.md`
  - `docs/16_策略与数据源管理说明.md`

### 设计说明

- 新闻源采用后端批量同步、本地入库、前端读库模式，不让前端直接请求聚合数据接口。
- 第一版新闻只作为投研解释层和事件输入，不直接参与买卖决策。
- 聚合数据低配 50 次/天足够第一版测试，建议每天 6-12 次，每次最多 50 条。
- 用户提供的聚合数据 token 不写入 Git，不写入文档，只保存到服务器数据源配置。

### 验收要点

- 前端构建通过。
- 后端语法编译通过。
- 数据源管理中应出现 `juhe_finance_news`。
- 填入 Key 后，`POST /api/news/sync` 可以同步新闻并写入 `news_article`。

## v0.62.1-editable-data-source-config - 2026-07-14

阶段：数据源管理从状态展示升级为可配置

### 完成内容

- 新增数据库表 `data_source_config`，用于保存数据源配置。
- 新增 alembic 迁移 `20260714_0007_data_source_config.py`。
- 数据源配置支持字段：
  - 数据源代码。
  - 数据源名称。
  - 数据源类型。
  - API 地址。
  - 鉴权方式。
  - 后端密钥字段。
  - 请求间隔。
  - 每分钟 / 每日限额。
  - 支持用途。
  - 业务适配状态。
  - 备注。
- 后端新增数据源 CRUD：
  - `GET /api/settings/data-sources`
  - `POST /api/settings/data-sources`
  - `PATCH /api/settings/data-sources/{provider_code}`
- 数据源管理页支持：
  - 新增数据源。
  - 编辑数据源配置。
  - 写入或替换密钥。
  - 清空数据库密钥。
  - 启用或停用数据源。
- Tushare 调用层支持读取数据库中的 `tushare` 配置：
  - 数据库 `secret_value` 优先于环境变量 `TUSHARE_TOKEN`。
  - 数据库 `base_url` 优先于环境变量 `TUSHARE_API_URL`。
- DeepSeek 调用层支持读取数据库中的 `deepseek` 配置：
  - 数据库 `secret_value` 优先于环境变量 `DEEPSEEK_API_KEY`。
  - 数据库 `base_url` 优先于环境变量 `DEEPSEEK_BASE_URL`。
- 新增数据源默认状态建议为 `metadata_only`，表示仅登记配置；真正用于行情同步前，需要后端适配字段口径和清洗规则。
- 更新 `docs/16_策略与数据源管理说明.md`，补充配置表、API、运行时覆盖规则和安全边界。

### 设计说明

- 前端可以提交新密钥，但接口永远不返回密钥明文，只返回脱敏值。
- 允许配置新的数据源，但不会自动把未知数据源用于正式行情同步，避免污染 `market_data_clean`。
- 当前真正接入运行时的数据源只有 `tushare` 和 `deepseek`；其他新增源需要后续开发适配器。

### 验收要点

- 前端构建通过。
- 后端语法编译通过。
- 数据源管理页可以打开新增 / 编辑弹窗。
- Tushare 和 DeepSeek 支持通过数据库配置覆盖环境变量。

## v0.62.0-strategy-data-source-management - 2026-07-14

阶段：策略管理和数据源管理后台化

### 完成内容

- 后端策略配置接口扩展：
  - `GET /api/strategies`
  - `POST /api/strategies`
  - `PATCH /api/strategies/{strategy_code}`
  - `POST /api/strategies/run`
- 系统会自动补齐 4 个内置策略：
  - `core_etf_rotation` 核心 ETF 轮动策略。
  - `aggressive_equity_rotation` 进取权益轮动策略。
  - `defensive_all_weather` 防守全天候配置策略。
  - `global_qdii_rotation` QDII 全球 ETF 轮动策略。
- 策略运行逻辑从固定权重升级为读取 `strategy_config.config.target_weights`。
- QDII/全球策略支持 `preferred_regions` 区域偏好参数。
- 新增前端“策略管理”页面：
  - 查看策略列表。
  - 新增策略。
  - 编辑策略名称、版本、调仓频率、目标权重、风险画像、区域偏好和说明。
  - 启用/停用策略。
  - 直接运行策略。
- “目标组合”页面策略代码输入框改为读取后端策略列表的下拉选择。
- 新增后端数据源状态接口：`GET /api/settings/data-sources`。
- 新增前端“数据源管理”页面：
  - 展示默认日历源、行情源、资料源、AI 源。
  - 展示 Tushare 和 DeepSeek 配置状态。
  - 展示脱敏 Token、API 地址、请求间隔和使用模块。
  - 明确 Token 不在前端保存。
- 左侧菜单新增“策略管理”和“数据源管理”入口。
- 新增 `docs/16_策略与数据源管理说明.md`。
- 更新 `docs/00_文档导航.md`、`docs/04_API接口文档.md`、`docs/05_策略设计.md`。

### 设计说明

- 当前新增策略属于“参数型策略”：共用 `factor_rotation` 引擎，但通过权重、风险画像和区域偏好产生不同组合。
- 真正完全不同的算法型策略，后续仍需要在 `backend/app/services/strategy_service.py` 中新增独立引擎或分发逻辑。
- 数据源管理页只负责展示配置状态，不把 Tushare Token 或 DeepSeek Key 明文交给浏览器。
- 当前正式行情、日历和 ETF 资料仍保持 Tushare-only，避免多数据源口径混用。

### 验收要点

- 前端构建通过。
- 策略管理页可以加载内置策略。
- 新增/编辑策略后，全流程和目标组合页面可以选择启用策略。
- 数据源管理页可以显示 Tushare、DeepSeek 的配置状态和脱敏 Token。
- 后端测试环境当前本机未安装 `pytest`，需以后在完整 Python 环境或容器内补跑自动化测试。

## v0.61.6-unified-analysis-period - 2026-07-14

阶段：统一全站分析周期口径

### 完成内容

- 新增前端公共日期工具 `frontend/src/datePresets.ts`，统一维护分析周期选项、标签和日期范围计算。
- 将以下页面从分散的“日期范围 / 快捷范围”改为统一“分析周期”口径：
  - 全流程。
  - 数据健康。
  - 因子排名。
  - 因子研究实验室。
  - ETF 对比。
  - ETF 筛选。
  - ETF 详情。
  - AI 投研委员会。
  - 回测。
- 统一分析周期选项：
  - 半年。
  - 1 年。
  - 3 年。
  - 5 年。
  - 成立以来。
  - 自定义。
- 数据健康额外保留 1 个月、3 个月、10 年，用于日常补数和历史初始化。
- 只有选择“自定义”时才显示日期范围选择器。
- 新增 `docs/15_分析周期口径说明.md`，详细说明分析周期、定投周期、持有周期的区别。
- 更新 `docs/00_文档导航.md` 和 `docs/10_系统流程说明.md`，加入分析周期文档入口和全流程说明。

### 设计说明

- 分析周期统一定义为历史回看窗口，不代表未来持有时间，也不代表定投执行时间。
- 定投计划继续使用“投入周期 / 计划周期”，避免和历史分析窗口混淆。
- 各页面不再各自手写日期选项，后续新增页面应引用 `frontend/src/datePresets.ts`。
- “成立以来”在纯前端页面按近 10 年窗口兜底；全流程后端仍会根据 ETF 上市日期或本地最早行情解析真实起点。

### 验收要点

- 前端构建通过。
- 因子排名、因子研究、ETF 对比、ETF 筛选、AI 投研、回测、数据健康、全流程均显示“分析周期”。
- 自定义日期只在选择“自定义”时显示。
- 文档包含分析周期专题说明。

## v0.61.5-workflow-guardrails-simplified - 2026-07-14

阶段：全流程参数简化与策略选择优化

### 完成内容

- 全流程页面移除“最少样本”手动输入。
- 系统根据分析周期自动设置样本门禁：
  - 最近半年：60 根日线。
  - 最近 1 年：120 根日线。
  - 最近 3 年：200 根日线。
  - 最近 5 年：250 根日线。
  - 成立以来：200 根日线。
  - 自定义周期：按日期跨度自动估算，范围限制在 20 到 250 根。
- 流程提示中展示当前自动样本门禁，用户不需要手动维护。
- 全流程“策略代码”改为“策略”下拉框，从后端内置策略配置读取。
- 如果策略列表读取失败，前端会回退到默认 `core_etf_rotation`，避免页面不可用。

### 设计说明

- 样本门禁属于数据质量控制，不适合作为普通用户的主要输入项。
- 新发 ETF 若历史样本不足，严格门禁会继续保护流程，避免用极短历史生成不稳健建议；非严格模式下可用于测试跑通。
- 策略是系统内置配置，不应像普通文本一样让用户随意输入，避免输错代码导致流程失败。
- 组合市值仍保留为用户输入，因为它代表本次模拟组合规模，会影响调仓金额估算。

### 验收要点

- 全流程页面不再显示“最少样本”输入框。
- 策略使用下拉框选择。
- 创建任务时仍会向后端传入自动计算后的 `minimum_history_bars`。
- 前端构建通过。

## v0.61.4-workflow-date-presets - 2026-07-14

阶段：全流程分析周期选择优化

### 完成内容

- 全流程页面将“日期范围”改为“分析周期”。
- 新增固定周期选项：
  - 最近半年。
  - 最近 1 年。
  - 最近 3 年。
  - 最近 5 年。
  - 成立以来。
  - 自定义。
- 只有选择“自定义”时才显示手动日期范围，普通用户不用再手动选起止日期。
- 后端 `WorkflowRunRequest` 新增 `date_preset` 字段，支持 `6m / 1y / 3y / 5y / inception / custom`。
- 固定周期默认结束日改为“最近一个已完成交易日”，避免 Tushare 当天日线未出导致数据新鲜度门禁失败。
- “成立以来”会先取本次研究范围内 ETF 的最早上市日期；若缺失上市日期，则取本地最早行情日期；仍缺失时回退 10 年。
- 全流程结果中记录实际解析后的 `start_date` 和 `end_date`，方便任务追溯。

### 设计说明

- 全流程日期不是未来持有期，而是历史回看窗口；固定周期可以降低普通用户理解成本。
- 对 ETF 日频策略来说，默认最近 1 年更适合作为起步分析窗口。
- 默认结束到最近已完成交易日，可以减少“今天未收盘 / Tushare 未更新”造成的误报。

### 验收要点

- 全流程默认显示“最近 1 年”，不再直接显示日期范围。
- 选择“自定义”后才显示日期范围选择器。
- 固定周期任务可以不传 `start_date/end_date`。
- 前端构建通过。

## v0.61.3-workflow-scope-selector - 2026-07-14

阶段：全流程研究范围选择优化

### 完成内容

- 全流程页面新增“研究范围”选择：
  - 核心池：当前持仓 + 目标组合 + 定投计划 + 启用 ETF。
  - 当前持仓。
  - 目标组合。
  - 定投计划。
  - 启用 ETF。
  - 全市场基础池。
  - 自定义代码。
- 默认研究范围改为“核心池”，普通用户不再需要逐个输入 ETF 代码。
- ETF 代码输入框只在选择“自定义代码”时显示，减少误操作。
- “同步数量”默认从 10 提高到 20，并保留最大 50 的保护，避免一次任务过大。
- 后端 `WorkflowRunRequest` 新增 `sync_scope` 字段，全流程任务会按范围自动解析 ETF 列表。
- 自定义代码仍然优先使用用户手动输入；非自定义范围下由系统自动取对应池子。

### 设计说明

- 全流程的目标是“一键完成数据、因子、策略、风控、调仓和报告”，不应该要求普通用户每次手动填写 ETF 代码。
- 对日常使用来说，“核心池”最符合直觉：优先覆盖当前持仓、目标组合、定投计划和已启用研究 ETF。
- “自定义代码”保留给临时测算少数 ETF 的场景。

### 验收要点

- 全流程页面默认不显示 ETF 代码输入框。
- 直接点击“创建后台任务”时，应按核心池自动选择 ETF。
- 选择“自定义代码”后，才显示手动输入框。
- 前端构建通过。

## v0.61.2-etf-profile-sync-polish - 2026-07-14

阶段：Tushare 补全结果校准与同步体验修复

### 完成内容

- 使用生产环境 Tushare 代理补全启用研究池 ETF 主资料。
- 启用研究池共 64 只 ETF：
  - 基金公司缺失：0。
  - 管理费缺失：0。
  - 托管费缺失：0。
  - 综合费率缺失：0。
  - 基金规模缺失：0。
  - 最新折溢价率缺失：0。
  - 跟踪误差缺失：32。
- 对 159338、159561、159625、159867、513880 等样本不足的启用 ETF 补跑三年历史日线，解决“样本少于 60 个工作日”的基础行情缺口。
- 修复 Tushare 增量行情同步体验：如果本地已有最新交易日数据，而 Tushare 对当天或非交易日尚无 `fund_daily` 返回，系统会标记为 `up_to_date_no_new_tushare_daily`，不再误记为同步失败。
- ETF 池资料完整度口径调整：
  - 基础完整度不再把“跟踪误差”作为硬性扣分项。
  - 仅缺跟踪误差时显示“核心完整”。
  - 缺失说明中明确提示：跟踪误差依赖跟踪指数行情映射，跨境、债券、黄金或细分主题 ETF 可能需要额外指数源。

### 设计说明

- Tushare `fund_basic`、`fund_nav`、`fund_share` 已足够支撑 ETF 基础档案、费率、规模和日频折溢价。
- 跟踪误差不是 ETF 基础资料字段，而是二次计算指标；它依赖 ETF 日线与跟踪指数日线的交集。
- 当前剩余缺口主要不是 ETF 主资料缺失，而是跟踪指数名称到 Tushare 指数代码的映射不足，尤其集中在 QDII、黄金、债券、货币和行业主题指数。
- 对普通用户筛选 ETF 来说，“基金公司、上市日期、费率、规模、折溢价、日线行情”应作为核心可用标准；跟踪误差作为增强指标逐步补齐。

### 验收要点

- 数据健康增量同步不应因为当天 Tushare 尚无日线而大量报错。
- ETF 池中仅缺跟踪误差的 ETF 应显示“核心完整”，而不是“待补档案”。
- 前端构建通过。
- 后端行情同步测试通过。

## v0.61.1-production-data-audit - 2026-07-14

阶段：生产数据审计与历史公开源残留清理

### 审计范围

- ETF 主资料：
  - 管理费、托管费、综合费率。
  - 基金规模。
  - 最新折溢价率。
  - 跟踪误差。
  - 名称缺失。
- 行情数据：
  - 原始行情来源分布。
  - 清洗行情状态分布。
  - 价格小于等于 0。
  - OHLC 高低价反转。
- 净值折溢价：
  - 净值小于等于 0。
  - 市价小于等于 0。
  - 折溢价率绝对值超过 20%。

### 审计结果

- ETF 总数：2141。
- 启用研究：64。
- 异常费率：0。
- 异常规模：0。
- 异常折溢价率：0。
- 异常跟踪误差：0。
- 名称缺失：0。
- 异常清洗价格：0。
- OHLC 高低价反转：0。
- 异常净值折溢价记录：0。
- 原始行情来源清理后只剩 `tushare`，共 86145 条。
- 清洗行情状态：
  - `normal`：86104。
  - `index`：534。
  - `warning`：41。

### 清理内容

- 删除旧版本当前持仓模块通过 `eastmoney_spot` 写入的 7 条原始行情。
- 同步删除这 7 条对应的清洗行情。
- 这些记录日期为 2026-07-11，且没有同日 Tushare 原始行情对应，清理后避免 Tushare-only 模式下继续误用公开源现价。

### 说明

- 41 条 `warning` 清洗行情均来自 Tushare 原始数据，主要是大波动或质量检查标记，不是公开源污染。
- 历史同步日志中仍保留旧版本 `akshare/eastmoney` 记录，用于追溯，不代表当前入口仍可调用。

## v0.61.0-tushare-only-data-mode - 2026-07-14

阶段：数据源切换为 Tushare-only，防止公开源口径污染

### 完成内容

- ETF 基础池同步、ETF 主资料补全、行情同步、交易日历同步均切换为 Tushare-only。
- 后端会拒绝 `source=akshare` 和 `source=eastmoney`，避免 AKShare/东方财富字段口径混入正式数据。
- 前端 ETF 池“同步基础池 / 补全资料”显式使用 `source=tushare`。
- 数据健康和全流程页面移除 AKShare/Eastmoney 选项，只保留 Tushare。
- 当前持仓自动补行情关闭东方财富现价兜底，只使用 Tushare 日线。
- 新增费率合理性保护：
  - 单项管理费/托管费超过 2% 不写入。
  - 综合费率超过 3% 不写入。
  - 避免把异常字段或公开源字段误写成 5% 这类不合理 ETF 费率。
- 保留旧 AKShare/东方财富函数代码，但入口禁用，后续如需恢复必须显式改开关。

### 设计说明

- Tushare `fund_basic.m_fee=0.50` 表示 0.50%，系统落库为 `0.005`，前端显示为 `0.500%`。
- 如果页面出现 5% 费率，通常是历史脏数据或公开源字段口径误写，不能继续让公开源参与主资料补全。
- 当前系统定位为日频 ETF 配置平台，先统一使用 Tushare 保证口径一致。

### 验收要点

- 前端不应再出现 AKShare/Eastmoney 数据源选项。
- API 传 `source=akshare` 或 `source=eastmoney` 应返回错误。
- 新同步/补全不应写入超过合理阈值的 ETF 费率。

## v0.60.0-index-tracking-error - 2026-07-14

阶段：指数行情与 ETF 跟踪误差计算

### 完成内容

- 新增 Tushare `index_daily` 封装，用于获取指数日线行情。
- 新增 `tracking_service`：
  - 支持常见 A 股宽基、行业和主题指数名称映射到 Tushare 指数代码。
  - 指数行情写入 `market_data_clean`，代码使用 `IDX:` 前缀，例如 `IDX:000300.SH`。
  - 基于 ETF 日收益与跟踪指数日收益的差值，计算年化跟踪误差。
- ETF 主资料补全流程在 `source=tushare` 或 `source=auto` 时，如果 ETF 缺少 `tracking_error`，会尝试自动补指数行情并计算跟踪误差。
- 为了节省 Tushare 请求，`preserve_existing=true` 且已有跟踪误差时不会重复请求指数行情。
- 新增跟踪服务单元测试，覆盖指数名称匹配、指数行情入库格式和跟踪误差计算。

### 设计说明

- 指数行情先复用 `market_data_clean`，避免新增迁移；使用 `IDX:` 前缀和 ETF 行情区分。
- 跟踪误差采用日频口径，适合每日收盘后更新，不涉及实时行情。
- 首批优先覆盖沪深300、中证500、中证1000、中证A500、上证50、创业板、科创50、红利、消费、证券、银行、新能源、芯片等常见 ETF 指数。

### 验收要点

- 对已同步 ETF 日线且能匹配指数代码的 ETF，资料补全后应写入 `tracking_error`。
- 指数行情应以 `IDX:指数代码` 形式进入 `market_data_clean`。
- 容器内测试应通过。

## v0.59.0-tushare-nav-share-profile - 2026-07-14

阶段：Tushare 日频净值/份额补全 ETF 规模

### 完成内容

- `POST /api/assets/sync-profiles` 在 `source=tushare` 或 `source=auto` 时新增调用 Tushare `fund_nav` 和 `fund_share`。
- 使用日频单位净值和基金份额估算基金规模：
  - `基金规模 = 单位净值 × 基金份额 × 10000`。
  - Tushare `fund_share.fd_share` 为万份口径。
- 如果数据库已有该 ETF 的日线收盘价，会用 `收盘价 / 单位净值 - 1` 计算日频折溢价率。
- 折溢价计算结果写入 `asset_master.latest_premium_rate`，并同步落到 `etf_nav_premium` 表，便于未来做历史折溢价分析。
- Tushare 主资料说明文案改为：规模可由 `fund_nav/fund_share` 补充，跟踪误差需指数行情计算。
- 新增单元测试覆盖净值/份额规模换算和最新日期选择。

### 设计说明

- 本系统当前定位为日频 ETF 配置和投研平台，不优先接盘中实时行情。
- `fund_nav` 和 `fund_share` 可以解决 ETF 池中“规模净值”维度长期停留在 0% 的问题。
- 折溢价率采用日频口径，适合每日收盘后更新和第二天投研判断；不用于盘中套利。

### 验收要点

- 对有净值和份额的 ETF 执行资料补全后，`fund_size` 应被写入。
- 若该 ETF 已有收盘行情，`latest_premium_rate` 和 `etf_nav_premium` 应同步更新。
- 本地 Python 编译通过，服务器容器测试通过。

## v0.58.0-etf-profile-completeness-breakdown - 2026-07-14

阶段：ETF 池资料完整度解释优化

### 完成内容

- ETF 池不再只展示单一“资料完整度百分比”，新增悬停明细。
- 资料完整度拆成 4 类：
  - 基础档案：基金公司、跟踪指数、上市日期、说明。
  - 费率资料：管理费、托管费、综合费率。
  - 规模净值：基金规模、最新折溢价率。
  - 跟踪质量：跟踪误差。
- ETF 池筛选项从“较完整 / 待补充”升级为：
  - 基础可用。
  - 待补规模/净值。
  - 待补跟踪指标。
  - 资料待补充。
- 表格中的资料列会显示“基础可用 / 完整 / 待补档案 / 待建档”等状态，避免用户误解大量 ETF 显示 70% 是系统错误。
- 完整度弹层明确说明：Tushare 基础档案补齐后通常约为 70%，剩余多为规模、折溢价率和跟踪误差。

### 设计说明

- 当前 70% 是字段覆盖率，不是投资质量评分。
- 只做日频 ETF 配置平台时，实时行情不是优先项；更应该优先补净值、份额、规模和指数行情。
- 本次先优化展示层，不改变数据库结构，为后续接入 `fund_nav`、`fund_share`、指数行情和跟踪误差计算预留解释框架。

### 验收要点

- ETF 池资料完整度列应显示总分和状态。
- 鼠标悬停资料完整度，应看到 4 类完整度、缺失字段和 70% 形成原因。
- 前端构建应通过。

## v0.57.0-tushare-etf-profile-sync - 2026-07-14

阶段：ETF 主资料补全接入 Tushare fund_basic

### 完成内容

- `POST /api/assets/sync-profiles` 支持 `source=tushare` 和 `source=auto`。
- Tushare 资料补全会写入基金公司、上市日期、管理费、托管费、综合费率、业绩基准/跟踪指数和说明。
- ETF 池页面“补全当前筛选资料”改为 `source=auto`，优先使用 Tushare 基金档案，再叠加其他来源可用字段。
- 增加 Tushare 业绩基准清洗逻辑，例如 `标普500指数×100%` 会转成更适合展示和同指数匹配的 `标普500`。
- 修复 Tushare 费率边界换算：`0.05` 应识别为 `0.05%` 并落库为 `0.0005`，避免托管费被放大 100 倍。
- 更新 API 文档，说明 Tushare `fund_basic` 能补哪些字段，以及哪些字段仍需要其他数据源。

### 设计说明

- Tushare `fund_basic` 有基金公司、上市日期、管理费、托管费、业绩基准等 ETF 档案字段。
- Tushare `fund_basic` 通常不提供实时基金规模、跟踪误差、实时折溢价率。
- 资料完整度低不是因为 Tushare 完全没有 ETF 信息，而是之前资料补全接口只走了 AKShare/Eastmoney 分支，没有把 Tushare 基础档案写回。

## v0.56.0-etf-detail-quant-ux - 2026-07-14

阶段：ETF 详情页量化决策体验优化

### 完成内容

- ETF 详情页明确区分“分析周期”和“计划持有”，避免把 1 年、3 年历史回看误解为持仓周期。
- 新增计划持有选项：1 个月、3 个月、6 个月、1 年+。
- 买入决策卡新增“计划持有”解释，根据买入评分和用户计划周期给出更贴近使用场景的提示。
- 将原“最新因子”改为“量化体检”，用收益、年化收益、波动、最大回撤、夏普、交易性、数据质量和 Alpha 解释替代裸因子表。
- 将“最近行情”改名为“原始行情抽样”，明确它主要用于排查数据，不作为普通用户首要决策区。
- 第一屏核心指标新增年化收益和夏普比率，让收益、回撤和风险调整表现一起展示。

### 高级量化视角调整

- 删除或弱化：裸因子表、原始行情表在决策中的优先级。
- 修改：周期控件命名为历史分析窗口，而不是持仓周期。
- 新增：计划持有周期、量化体检、指标解释、周期误解提示。

### 验收要点

- 用户能明确知道 6 个月、1 年、3 年是历史回看周期。
- 用户可以单独选择计划持有周期。
- 详情页应先给决策，再给理由、风险、量化指标和原始数据。

## v0.55.0-data-quality-status-scope - 2026-07-14

阶段：数据健康页统计口径修复

### 完成内容

- 数据健康页顶部状态从“全库历史累计”改为“最近一次检查批次”。
- 顶部卡片新增历史累计说明，方便区分当前问题和历史日志。
- `GET /api/data-quality/status` 新增 `history_total_logs`、`history_error_logs`、`history_warning_logs`。
- 质量检查前会清理同一 ETF、同一区间的旧质量日志，避免重复检查导致告警数量不断膨胀。
- 更新 API 文档，明确最近批次统计和历史累计统计的区别。

### 修复原因

- 旧版本顶部状态直接统计 `data_quality_log` 全表。
- 多次检查缺行情时，会为同一 ETF、同一日期重复写入 warning。
- 页面容易显示几万条警告，让用户误以为当前同步任务全部异常。

### 验收要点

- 数据健康页“最近错误 / 最近警告 / 最近日志”应只代表最新一次检查。
- 历史累计仍可看到，但不再作为当前整体状态。
- 对同一区间重复执行检查，不应继续无限累加同一批缺失数据日志。

## v0.54.0-etf-detail-decision-panel - 2026-07-14

阶段：ETF 详情页从“数据详情”升级为“买入决策页”

### 完成内容

- `GET /api/etf-detail/{symbol}` 新增 `decision` 结构化决策摘要。
- 决策摘要包含系统行动建议、买入评分、置信度、建议仓位、买入节奏、风险阈值、数据质量、核心理由、主要风险和下一步操作。
- ETF 详情页第一屏改为“买入决策”面板，普通用户打开后优先看到“能不能买、怎么买、风险在哪”。
- 详情页新增 `6个月 / 1年 / 3年 / 自定义` 周期切换，适合评估短期持有或长期配置。
- 详情页同步行情默认改用 Tushare，并保留单只 ETF、增量、清洗后刷新，避免用户手动跳转到数据健康。
- 保留原有净值回撤图、ETF 主资料、最新因子、同指数 ETF 替代和最近行情。
- 更新 `docs/04_API接口文档.md`，补充 ETF 详情接口的 `decision` 返回结构。

### 设计说明

- 本版不改变底层买入评分算法，只把已有评分、回撤、波动、交易性、数据质量和同指数候选整理成普通用户可理解的决策语言。
- `decision` 只生成建议，不连接券商，不自动买卖。
- 详情页仍以本地清洗行情为依据；行情不足时会明确提示先同步数据。

### 验收要点

- 打开任意 ETF 详情页，应先看到买入决策卡，而不是只有行情指标。
- 切换 6 个月、1 年、3 年后，页面应刷新对应区间的收益、回撤和决策摘要。
- 后端编译和前端构建应通过。

## v0.53.0-sp500-user-flow-optimization - 2026-07-13

阶段：普通用户标普 500 买入评估流程优化。

### 完成内容

- 以普通用户身份测试“准备买入一只标普 500 ETF，计划持有 6 个月”的完整流程。
- 新增使用报告 `docs/14_普通用户标普500评估报告.md`，记录用户路径、测算结论、体验问题和高级开发优化项。
- ETF 对比页新增“最近 6 个月”快捷日期窗口。
- ETF 筛选页新增“最近 6 个月”快捷日期窗口。
- `POST /api/etf-compare` 和 `POST /api/etf-compare/screener` 新增 `auto_sync_missing` 和 `max_auto_sync_symbols` 参数。
- 当用户开启自动补数时，系统会对本次分析中样本不足的少量 ETF 自动尝试用 Tushare 补齐所选区间行情。
- 自动补数默认关闭，避免批量筛选时误耗共享数据源。

### 测试结论

- `513500 博时标普500ETF(QDII)` 补齐近一年行情后，最近 6 个月样本数 120。
- 最近 6 个月区间收益约 2.60%，年化约 5.29%，最大回撤约 -12.64%，夏普约 0.18。
- 系统买入评分为 59，候选等级为“谨慎观察”。

## v0.52.0-holding-auto-enable - 2026-07-13

阶段：当前持仓上线体验加固。

### 完成内容

- 保存当前持仓快照前，系统会自动规范化持仓代码并准备持仓 ETF 主数据。
- 已存在于 ETF 池但未启用的持仓 ETF，会自动设置为 `enabled=true`，进入研究范围。
- 不在 ETF 池里的持仓 ETF，会自动登记到 `asset_master` 并启用研究。
- 缺少清洗行情的持仓 ETF，会自动尝试同步最近 45 天行情并清洗入库。
- 当前持仓页面文案更新，明确“持仓 ETF 会自动进入研究范围”。
- 新增单元测试覆盖持仓准备流程。

### 设计说明

- 这项能力解决普通用户录入持仓后还要手动去 ETF 池启用、去数据健康补行情的问题。
- 自动补行情只补最近短窗口，用于价格和持仓分析，不替代完整历史初始化。
- 若外部行情源失败，系统仍允许用户临时补现价保存持仓，后续可再到数据健康补完整行情。

## v0.51.0-etf-screener-and-launch-audit - 2026-07-13

阶段：上线终局审计与 ETF 筛选器。

### 完成内容

- 新增逐模块上线审计文档 `docs/13_上线终局审计.md`，按总览、ETF 池、筛选、对比、详情、数据健康、因子、策略、持仓、定投、风控、回测、报告、AI、部署安全等模块列出当前状态、主要不足和上线优先级。
- 新增后端接口 `POST /api/etf-compare/screener`，用于按范围批量筛选 ETF 候选。
- 筛选范围支持 `enabled`、`core`、`positions`、`target`、`plans`、`all` 和 `custom`。
- 筛选器支持最低样本数、最低交易性、最低买入评分、资产类别、返回数量等过滤条件。
- 新增前端 `ETF 筛选` 页面，作为普通用户判断“哪些 ETF 值得进一步研究”的第一入口。
- 修复 ETF 对比响应模型未声明 v0.50 新指标的问题，避免 FastAPI 过滤 `buy_score`、`sharpe_ratio` 等字段。
- 文档导航新增上线审计文档入口。

### 设计说明

- ETF 筛选器不直接生成买卖指令，只提供候选排序；买入前仍建议进入 ETF 对比、ETF 详情、AI 投研和回测继续验证。
- 筛选排序优先使用买入评分，其次考虑可交易性、夏普和年化收益。
- 当前上线审计把 P0 缺口明确为数据日志清理、持仓自动启用、组合约束、系统状态增强和备份恢复演练。

## v0.50.0-etf-quant-selection-upgrade - 2026-07-13

阶段：ETF 量化筛选能力补强。

### 行业视角发现的不足

- ETF 对比页原先更偏“行情展示”，缺少夏普、卡玛、下行波动、胜率等风险调整收益指标。
- “值得买”没有形成明确的候选等级，用户需要自己把收益、回撤、流动性和风险等级拼起来判断。
- 交易性和风险性已有基础指标，但缺少与买入决策直接关联的综合评分。
- 数据完整性只展示样本数，缺少估算缺口率，容易忽略历史数据不完整导致的指标偏差。

### 完成内容

- ETF 对比指标新增 `downside_volatility`、`sharpe_ratio`、`sortino_ratio`、`calmar_ratio`、`positive_day_rate`、`estimated_gap_ratio`。
- 新增 `buy_score`、`buy_level`、`buy_notes`，把收益、波动、回撤、夏普、交易性、风险等级和数据缺口合成一个买入候选评分。
- ETF 对比页新增“买入候选提示”，直接展示每只 ETF 的候选等级和主要短板。
- 对比表新增下行波动、夏普、卡玛、胜率、缺口率、买入评分列。
- 新增单元测试覆盖 ETF 对比新指标，避免后续回归。

### 设计说明

- `buy_score` 只用于排序和筛选，不是自动下单信号。
- 系统仍然坚持先看数据质量和交易性，再谈收益；样本不足、缺口率偏高或交易性偏弱会明显压低评分。
- 本版本不新增数据库字段，所有指标均由 `market_data_clean` 实时派生，部署风险低。

## v0.49.0-data-health-date-presets - 2026-07-13

阶段：数据健康页日期选择体验优化。

### 完成内容

- 数据健康页新增“日期模式”，支持在“快捷范围”和“自定义日期”之间切换。
- 快捷范围内置最近 1 个月、3 个月、1 年、3 年、5 年、10 年，减少日常同步和检查时反复手动选日期。
- 页面会展示当前实际使用的日期范围，避免误操作。
- 自定义日期范围保留原有日期选择器，并禁止清空，避免提交空日期导致同步或检查失败。
- 日期计算改成本地日期格式，降低时区边界导致日期偏移的概率。

### 使用建议

- 日常刷新行情：使用最近 1 个月。
- 普通数据健康检查：使用最近 1 年。
- 策略观察或回测前检查：使用最近 3 年或 5 年。
- 首次补历史数据或长期验证：使用最近 10 年。

## v0.48.0-historical-market-init - 2026-07-13

阶段：精选 ETF 十年历史行情初始化。

### 完成内容

- 新增后端任务 `historical_market_init`，专门用于批量初始化 ETF 长周期历史行情。
- 新增接口 `POST /api/workflows/historical-init`，支持按 `curated`、`enabled`、`core`、`positions` 或自定义代码范围创建历史行情初始化任务。
- 默认参数面向当前方案：精选 50 支 ETF、近 10 年、Tushare 数据源、全量回填、同步后自动清洗入库。
- 任务步骤固定为三步：同步交易日历、同步历史日线行情、检查历史行情完整性。
- 历史行情继续写入原有 `market_data_raw` 和 `market_data_clean`，不会新建重复行情表。
- 前端 API 客户端新增 `startHistoricalMarketInit`，后续可接入数据健康页按钮。
- 新增单元测试，覆盖历史初始化步骤顺序、默认日期窗口、精选 50 支范围解析。

### 设计说明

- 本版本同步的是 ETF 日线行情，不包含历史分钟、实时盘口、Level-2、资金流向。
- 当前优先保证精选 50 支 ETF 的长期样本充足，满足因子、策略、回测、AI 投研和定投计划分析。
- 历史初始化默认关闭增量同步，避免已有少量最近行情时跳过早期历史数据。
- 全市场 ETF 基础档案仍保存在 `asset_master`；只有进入精选池或用户启用的 ETF 才建议同步完整历史行情。
- 如果未来新增策略只依赖日线 OHLCV、成交额、复权净值推导指标，通常不需要重跑 10 年日线；如果新增规模、份额、折溢价、成分、资金流等因子，则需要补充对应专题表。

### 验收要点

- 调用 `POST /api/workflows/historical-init` 后应生成一个工作流任务。
- 任务完成后，精选 ETF 在 `market_data_clean` 中应有近 10 年交易日行情；上市不足 10 年的 ETF 只会有上市后的数据。
- 数据健康页、ETF 详情、策略和回测不再因为精选 ETF 样本少于 60 个交易日而无法分析。

## v0.47.0-curated-etf-research-pool - 2026-07-13

阶段：精选 ETF 研究池与全市场基础库分层

### 完成内容

- 新增后端内置精选 ETF 研究池，共 50 支，覆盖 A 股宽基、行业主题、债券、货币、黄金、QDII 等类别。
- 新增 `POST /api/assets/seed-curated`，用于把内置精选 ETF 启用为研究池。
- ETF 全市场基础库仍保存在 `asset_master` 中，新同步 ETF 默认不启用研究。
- `GET /api/assets` 新增 `q` 和 `limit` 参数，支持按代码、名称、跟踪指数、基金公司搜索。
- 前端 ETF 池按钮改为调用后端内置精选池接口，避免只导入前端少量示例数据。
- 新增测试验证精选池数量和跨资产类别覆盖。

### 设计说明

- 全市场基础库用于“可搜索、可添加”，不直接进入策略。
- 精选研究池用于“优先同步行情、因子、策略、回测、AI 投研”，规模先控制在 50 支左右，便于测试稳定性。
- 用户后续搜索任意 ETF 代码或名称后，只需要把该 ETF 启用，即可加入研究池；完整行情和详情再按该 ETF 渲染和同步。

### 验收要点

- 点击 ETF 池页面的精选池按钮后，启用研究 ETF 数量应接近 50。
- 搜索 ETF 代码或名称后，可以找到全市场基础库中的 ETF。
- 新增 ETF 启用后，会进入数据健康、策略、回测和 AI 投研的 `enabled/core` 范围。

## v0.46.0-etf-universe-tushare-fallback - 2026-07-13

阶段：ETF 基础池全市场同步兜底源修复

### 完成内容

- ETF 基础池同步新增 Tushare `fund_basic` 数据源。
- `source=auto` 调整为：东方财富分页源 -> Tushare `fund_basic` -> AKShare。
- 当东方财富分页接口在服务器上偶发断连时，系统会自动切换 Tushare 基金基础信息，避免全市场同步直接失败。
- 新增 Tushare ETF 基础行解析逻辑，支持同步代码、名称、交易所、基金公司、跟踪标的、上市日期、管理费、托管费和综合费率。
- 保留“新同步 ETF 默认不启用研究”的规则，避免全市场 ETF 自动进入行情、因子、策略和回测流程。
- 更新 README、API 文档、数据库设计、系统流程说明和部署说明。
- 新增单元测试覆盖 auto 兜底顺序和 Tushare 字段映射。

### 设计说明

- Tushare `fund_basic` 只用于 ETF 档案基础池，不同步历史行情。
- Tushare 的 `issue_amount` 不等同当前基金规模，本版不写入 `fund_size`，避免产生错误规模数据。
- 日常行情同步仍建议使用 `core`、`positions`、`enabled` 等范围，避免共享 token 场景下产生过多请求。

### 验收要点

- 东方财富全量分页不稳定时，点击“同步 ETF 基础池”仍可通过 Tushare 兜底完成。
- 同步成功后，`asset_master` 中 ETF 数量应明显增加，并且 `asset_sync_log` 记录 `source=tushare` 或 `source=eastmoney`。
- `docker compose run --rm api pytest -q` 应通过。

## v0.45.0-etf-universe-sync-resilience - 2026-07-13

阶段：ETF 基础池同步稳定性修复

### 完成内容

- 将“同步全市场 ETF”语义调整为“同步 ETF 基础池”，更准确地区分 ETF 档案同步和历史行情同步。
- ETF 基础池同步默认使用 `source=auto`。
- AKShare/东方财富列表接口断连时，系统会自动重试 3 次。
- AKShare 仍失败时，自动切换到东方财富 `88.push2.eastmoney.com` 备用列表接口。
- 外部数据源失败时保留本地已有 ETF 池，不清空、不覆盖用户已维护资料。
- 同步成功或失败都会写入 `asset_sync_log`，ETF 池页面“同步日志”可查看基础池和资料补全记录。
- 前端错误提示改为中文可理解提示，避免直接暴露底层英文异常。

### 设计说明

- “ETF 基础池同步”只同步 ETF 档案列表，不同步所有历史行情。
- 历史行情仍由数据健康页、ETF 详情页或全流程任务按代码和日期范围同步。
- 当前不强依赖 Tushare 高积分接口；后续有 8000 积分权限后可把 Tushare `etf_basic` 接为高质量主数据源。

### 验收要点

- 点击“同步 ETF 基础池”时，AKShare 偶发断连不再直接导致整体失败。
- 若备用源可用，页面提示通过 `eastmoney` 同步成功。
- 若所有外部源都失败，页面展示中文提示，且本地 ETF 池不受影响。

## v0.44.0-holding-alternative-advice - 2026-07-13

阶段：持仓替代提示、AI 投研替代 Agent、月报替代观察

### 完成内容

- 持仓分析 API 返回同指数 ETF 替代候选，复用 ETF 详情页的替代推荐逻辑。
- 当前持仓页面在“持仓分析结果”中展示最佳同指数替代 ETF、推荐级别和原因。
- AI 投研新增“执行替代 Agent”，专门评估同指数 ETF 是否存在更优观察对象。
- DeepSeek LLM payload 新增 `same_index_alternatives`，让 AI 总结时能引用同指数替代候选。
- 月度 Markdown 报告新增“持仓替代观察 (Same-Index Alternatives)”小节。
- 更新前端样式，使替代推荐在表格中更易读。

### 设计说明

- 替代推荐仍只作为观察提示，不自动换仓，不自动下单。
- 持仓分析不新增数据库字段，替代候选在接口返回时动态计算，避免把可变评分固化到持仓分析表。
- AI 投研会提示是否值得进一步比较，但不会给出无条件换仓建议。

### 验收要点

- 运行持仓分析后，页面能看到同指数替代 ETF 提示。
- AI 投研结果包含“执行替代 Agent”。
- 月度报告包含“持仓替代观察”小节。
- 前端构建、后端测试和生产部署均应通过。

## v0.43.0-same-index-alternatives - 2026-07-13

阶段：同指数 ETF 替代推荐与同步日志

### 完成内容

- 新增 `asset_sync_log` 表，用于记录 ETF 主资料补全任务的来源、状态、总数、补全数、跳过数和失败数。
- 新增 `GET /api/assets/sync-logs`，前端可查看最近 ETF 主资料补全日志。
- ETF 池页面新增“资料补全日志”模块，展示最近补全任务状态。
- ETF 详情接口新增 `alternatives` 字段，返回同跟踪指数 ETF 候选。
- ETF 详情页新增“同指数 ETF 替代”表格，展示候选 ETF 的综合分、交易性、规模、费率、日均成交额和推荐原因。
- 新增推荐评分：综合考虑可交易性、基金规模和综合费率，并标记“首选关注 / 可替代 / 谨慎”。

### 设计说明

- 同指数替代只基于 `tracking_index` 精确匹配，不跨指数推荐，避免把不同风险暴露的 ETF 混在一起。
- 推荐结果仅作为观察和比较，不触发交易，也不代表确定性收益。
- 补全日志只保存摘要和最近结果，避免日志表无限膨胀或保存过多外部接口细节。

### 验收要点

- ETF 池完成资料补全后，页面能看到补全日志。
- ETF 详情页如果存在同跟踪指数 ETF，会展示候选列表和推荐原因。
- 前端构建、后端测试、数据库迁移和生产部署均应通过。

## v0.42.0-etf-profile-autofill - 2026-07-13

阶段：ETF 主资料自动补全

### 完成内容

- 新增 `POST /api/assets/sync-profiles`，支持按指定 ETF 代码批量补全主资料。
- 资料补全默认使用 AKShare 全市场 ETF 轻量列表，不消耗 Tushare token。
- 补全逻辑默认只填空字段，避免覆盖用户手工维护的基金公司、跟踪指数、规模、费率等数据。
- 新增 ETF 名称规则推断：基金公司、跟踪指数、资产类别、区域、风险等级和说明。
- ETF 池页面新增“补全当前筛选资料”按钮，一次最多处理当前筛选结果前 100 只 ETF。
- 新增主资料推断和行情字段解析单元测试。

### 设计说明

- 这一版优先解决“ETF 池/持仓录入后资料不完整”的问题。
- 由于部分外部详情接口较慢，本版先采用轻量列表 + 名称规则推断，保证页面操作稳定。
- 后续可继续接入更完整的基金资料源，补全上市日期、精确规模、费率、跟踪误差和溢价率。

### 验收要点

- ETF 池页面点击“补全当前筛选资料”后，空白资料字段会被批量补充。
- 已手工填写的资料不会被覆盖。
- 前端构建、后端测试和生产部署均应通过。

## v0.38.0-deepseek-agent-analysis - 2026-07-13

阶段：DeepSeek ETF 多 Agent 投研

### 完成内容

- 新增 DeepSeek 配置项：`DEEPSEEK_API_KEY`、`DEEPSEEK_BASE_URL`、`DEEPSEEK_MODEL`、`DEEPSEEK_TIMEOUT_SECONDS`、`DEEPSEEK_MAX_TOKENS` 和 `DEEPSEEK_THINKING_ENABLED`。
- 新增 DeepSeek OpenAI 兼容接口客户端，默认使用官方当前推荐的 `deepseek-v4-flash`，不再使用即将废弃的 `deepseek-chat`。
- 新增 `POST /api/agent-analysis/etf`，基于本地 ETF 详情、因子、当前持仓、目标组合和持仓分析生成多 Agent 投研结论。
- 新增六类 ETF Agent：市场环境、技术趋势、流动性、组合持仓、风险控制和复合经理。
- 新增前端“AI 投研”页面，支持输入 ETF 代码、日期范围、是否启用 DeepSeek 总结，并展示各 Agent 证据、风险和建议。
- DeepSeek 未配置或调用失败时，系统自动退回规则型总结，保证功能可用。

### 设计说明

- DeepSeek API key 只应写入本地 `.env` 或服务器 `.env.production`，不得提交到 Git 仓库。
- AI 投研当前只生成建议和解释，不自动下单，不连接券商交易接口。
- 第一版优先复用本地数据，新闻/事件 Agent、资金流 Agent 和 ETF 持仓结构 Agent 后续再接外部数据源。

### 验收要点

- 未配置 DeepSeek 时，AI 投研页面仍能返回规则型多 Agent 分析。
- 配置 DeepSeek 后，复合经理结论由 DeepSeek 生成自然语言总结。
- 前端构建、后端测试和生产部署均应通过。

## v0.37.1-etf-detail-sync-hint - 2026-07-13

阶段：ETF 详情行情同步提示

### 完成内容

- 排查线上 ETF 详情页“刷新不出数据”的原因：详情页只读取本地 `market_data_clean` 清洗行情，不会在普通刷新时自动请求外部数据源。
- 确认线上 `510300`、`511010` 当前没有清洗行情，因此详情曲线为空；全库当前只有少量 ETF 有行情记录。
- ETF 详情页新增“同步本 ETF 行情”按钮，使用 AKShare/Eastmoney 路径同步当前代码和当前日期范围，完成后自动刷新详情。
- 当当前日期范围没有曲线数据时，页面显示明确空状态提示，避免用户误以为页面卡住或接口异常。

### 设计说明

- 普通“刷新”继续只读取本地数据，避免每次打开详情页都触发外部请求。
- “同步本 ETF 行情”是用户显式动作，默认使用 `akshare`，不消耗 Tushare token。
- 同步范围固定为当前详情页代码和日期范围，避免误触发全市场大批量请求。

### 验收要点

- 没有本地行情的 ETF 详情页会显示空状态说明。
- 点击“同步本 ETF 行情”后，会补当前 ETF 行情并自动刷新详情。
- 前端构建和后端测试均应通过。

## v0.37.0-performance-observability - 2026-07-13

阶段：前端性能与运行可观测性

### 完成内容

- 前端路由改为页面级懒加载，避免首次进入系统时一次性加载所有业务页面。
- Vite 构建增加 `vue`、`element`、`charts` 和 `http` 手动分包，降低主入口 chunk 体积，便于浏览器缓存稳定依赖。
- 新增“系统状态”页面，集中展示 `/health`、`/health/live`、`/health/ready`、数据质量摘要、最近任务和异常日志。
- 左侧菜单新增“系统状态”入口，方便上线后快速判断 API、worker、数据质量或后台任务是否异常。
- 文档版本更新到 `v0.37.0-performance-observability`。

### 设计说明

- 系统状态页只读取现有健康检查、数据质量和工作流接口，不新增数据库结构。
- 页面使用 `Promise.allSettled` 聚合状态，单个接口失败不会导致整页空白，而是把失败原因展示在对应探针上。
- 分包优先拆稳定三方依赖，业务页面继续按路由懒加载；后续可进一步按重图表页面做更细粒度拆分。

### 验收要点

- 首屏入口包不再包含所有业务页面代码。
- 访问“系统状态”页可以看到 API 存活、就绪状态、数据质量和最近任务。
- 前端构建、后端测试和生产部署均应通过。

## v0.36.1-security-hardening - 2026-07-11

阶段：安全加固与漏洞巡检

### 完成内容

- 前端生产 Nginx 增加基础安全响应头：`Content-Security-Policy`、`X-Frame-Options`、`X-Content-Type-Options`、`Referrer-Policy` 和 `Permissions-Policy`。
- 登录限流的客户端识别逻辑加固：启用 `TRUST_PROXY_HEADERS` 时使用 `X-Forwarded-For` 右侧代理追加地址，降低用户伪造首段 IP 绕过限流的风险。
- 新增代理客户端识别单元测试，覆盖多段 `X-Forwarded-For` 场景。
- 执行前端依赖审计，`npm audit` 当前无已知漏洞。
- 执行后端依赖审计，修复 `python-dotenv`、`pytest`、`starlette` 和容器内 `pip` 已知漏洞；其中 `starlette` 通过升级 FastAPI 并显式固定安全版本完成。
- 执行仓库敏感信息巡检，未发现真实 Tushare token 或私钥被提交到代码库。

### 设计说明

- CSP 当前允许 `style-src 'unsafe-inline'`，用于兼容 Element Plus 和图表组件的运行时样式；脚本仍限制为 `script-src 'self'`。
- 生产环境仍建议只把真实密钥写入服务器 `.env.production`，不要写入 Git 仓库。
- 当前项目运行在外部 Caddy 反代后方，限流 IP 识别采用更保守的代理链右侧地址；后续若要精确识别真实用户 IP，应统一配置可信代理链。
- 后端依赖升级范围控制在 Web 框架、安全配置读取、测试工具和镜像构建工具链，不改变业务接口和数据库结构。

### 验收要点

- 生产响应头中可以看到 `Content-Security-Policy`、`X-Frame-Options` 等安全头。
- 前端构建、后端测试和生产部署均应通过。

## v0.36.0-etf-detail-page - 2026-07-11

阶段：ETF 单只详情画像

### 完成内容

- 新增 `GET /api/etf-detail/{symbol}`，聚合单只 ETF 的资产主数据、可交易性指标、最新因子、净值/回撤曲线和最近行情。
- 新增前端“ETF 详情”页面，支持按日期范围查看标准化净值、回撤、成交额、风险和因子画像。
- ETF 池、因子排名、ETF 对比页新增详情跳转入口。
- 新增详情曲线单元测试，覆盖标准化净值和回撤计算。

### 设计说明

- 详情页只读取本地 `asset_master`、`market_data_clean` 和 `factor_daily`，不额外请求外部数据源。
- 详情页为后续同类 ETF 推荐、报告增强和单只 ETF 诊断提供统一入口。

### 验收要点

- 从 ETF 池点击“详情”可进入 `/etf-detail/{symbol}`。
- 页面能展示单只 ETF 的净值/回撤曲线、可交易性评分、最新因子和最近行情。

## v0.35.0-tradability-constrained-strategy - 2026-07-11

阶段：可交易性评分接入策略降权/剔除

### 完成内容

- `core_etf_rotation` 目标组合生成时正式使用可交易性评分。
- 权益 ETF 评分低于 40：不进入权益候选目标组合。
- 权益 ETF 评分 40-60：目标权重减半，释放出的权重转入现金缓冲 ETF。
- 目标组合 `reason` 继续记录 `tradability_score` 和 `tradability_adjustment`，便于报告复盘。
- 新增策略权重测试，覆盖低分剔除和中低分降权场景。

### 设计说明

- 该约束当前只作用于权益 ETF，债券、黄金、现金暂不按评分降权。
- 降权释放的资金进入现金缓冲，而不是强行分配给其他高风险 ETF。
- 阈值先固定在后端：`<40` 剔除，`40-60` 降权，后续可做成策略参数配置。

### 验收要点

- 低评分权益 ETF 不会被策略选入目标组合。
- 中低评分权益 ETF 入选时权重会减半，现金缓冲权重增加。
- 目标组合说明里可以看到降权或无调整原因。

## v0.34.3-tradability-batch-performance - 2026-07-11

阶段：可交易性评分批量查询性能优化

### 完成内容

- 优化 `POST /api/etf-compare/tradability` 的数据库访问方式。
- 批量评分从“每只 ETF 单独查询行情”改为“一次查询全部行情后按代码分组”。
- 保留无行情 ETF 的评分结果，避免前端 ETF 池遗漏未同步行情的代码。

### 设计说明

- 本版不改变评分公式和业务口径，只优化全市场 ETF 池变大后的查询性能。
- 该优化主要服务 ETF 池、因子排名页和后续策略前检查的批量评分场景。

### 验收要点

- ETF 池和因子排名页仍能正常展示可交易性评分。
- 大批量 ETF 评分时数据库查询次数显著减少。

## v0.34.2-tradability-factor-strategy - 2026-07-11

阶段：可交易性评分接入因子排名和策略解释

### 完成内容

- 因子排名页新增可交易性评分列，展示评分、等级和提示说明。
- 策略生成目标组合时，会在 `target_portfolio.reason` 中附带入选 ETF 的可交易性评分。
- 报告和目标组合页读取 reason 时，可以看到入选 ETF 的流动性/样本提示。

### 设计说明

- 当前仍不直接按可交易性评分降权或剔除 ETF，先以提示方式接入。
- 后续可在评分规则稳定后，把低于阈值的 ETF 自动降权、剔除或要求人工确认。

### 验收要点

- 因子排名表能看到每只 ETF 的可交易性评分。
- 运行策略后，目标组合说明中包含 `tradability_score`。

## v0.34.1-tradability-integration - 2026-07-11

阶段：可交易性评分接入 ETF 池和工作流门禁

### 完成内容

- 新增 `POST /api/etf-compare/tradability`，用于批量计算 ETF 可交易性评分。
- ETF 池页面新增“可交易性”列，展示评分、等级和提示说明。
- ETF 池页面新增“评分下限”筛选，用于快速排除样本不足或成交额偏低的 ETF。
- 全流程市场门禁结果新增 `tradability_score`、`tradability_level`、`tradability_notes` 和 `weak_tradability` warning。

### 设计说明

- 当前可交易性评分作为提示和筛选条件，不作为硬拦截条件，避免粗规则误伤。
- 评分来源仍是本地清洗行情，不会在浏览 ETF 池时额外请求外部行情源。

### 验收要点

- ETF 池列表可以看到每只 ETF 的可交易性评分。
- 调整评分下限后，ETF 池列表会按评分筛选。
- 全流程市场步骤结果中能看到低评分 ETF 的 warning。

## v0.34.0-etf-compare-dca-report - 2026-07-11

阶段：ETF 对比、可交易性评分、定投和报告增强

### 完成内容

- 新增 `POST /api/etf-compare`，基于清洗行情计算 ETF 对比指标。
- 新增前端“ETF 对比”页面，展示标准化净值曲线、收益、年化波动、最大回撤、日均成交额、样本数、相关性矩阵和可交易性评分。
- 新增可交易性评分规则：根据历史样本数、日均成交额和零成交量日期给出 0-100 分与中文等级。
- 增强定投建议：固定节奏、回撤增强、信号择时、未来自动执行预览会生成不同资金分配逻辑和说明。
- 增强月度报告：新增 ETF 池状态、当前持仓快照、定投建议章节。
- 新增 `scripts/run_etf_universe_sync.py`，用于服务器 cron/systemd timer 低频同步 ETF 全市场基础池。

### 设计说明

- ETF 对比页不主动拉行情，只读取 `market_data_clean`，避免用户误以为点击对比会大量请求外部数据源。
- 可交易性评分先做工程化粗筛，不直接决定买卖；后续可接入规模、份额、溢价率、跟踪误差和基金公司信息。
- 定投目标年化是资金节奏参数，不是收益承诺；系统仍然只输出建议，不自动下单。

### 验收要点

- 左侧菜单出现“ETF 对比”，可输入多只 ETF 查看曲线、指标和相关性。
- 定投建议原因中会体现执行模式和目标年化约束。
- 新生成月报包含当前持仓和定投建议章节。

## v0.33.0-etf-universe-sync - 2026-07-11

阶段：ETF 全市场基础池同步

### 完成内容

- 新增 `POST /api/assets/sync-universe`，支持通过 AKShare 同步全市场 ETF 基础主数据。
- 新同步的 ETF 默认不启用研究，避免全市场资产一次性进入行情同步、因子计算、策略和回测。
- 已存在 ETF 再次同步时会更新名称、交易所、分类、区域等基础信息，但保留原有启用状态、风险等级和说明。
- ETF 池页面新增“同步全市场 ETF”按钮，用于把外部 ETF 列表导入基础池。
- 新增 ETF 主数据映射和粗分类测试，覆盖境内权益、跨境 QDII、债券、黄金和代码归一化。

### 设计说明

- `asset_master` 现在承担两层含义：全市场基础池和启用研究池。
- `enabled=false` 表示“系统知道这只 ETF，但暂不纳入研究”；`enabled=true` 才进入日常行情、因子、策略和回测范围。
- AKShare 不需要 token，但依赖公开行情源，偶发不可用时接口返回 502，重试即可。

### 验收要点

- ETF 池页面点击“同步全市场 ETF”后，主数据列表数量增加。
- 已经启用的 ETF 在同步后仍保持启用状态，手动维护的风险等级和说明不会被覆盖。
- 用户新增持仓时，如果代码在基础池中，名称和类型可以直接按代码补全。

## v0.32.4-holding-quote-fallback - 2026-07-11

阶段：新增持仓现价兜底

### 完成内容

- 当前持仓自动补全时，如果最近日线行情没有补到，会继续调用东方财富单只 ETF 最新报价作为现价兜底。
- 最新报价会写入 `market_data_raw` 和 `market_data_clean`，使持仓市值、盈亏和权重能立即计算。
- 如果报价接口也失败，不抛 500，继续允许用户临时填写现价保存。
- 新 ETF 自动登记为候选 ETF 后，如果报价返回名称，会用真实名称替换 `代码 ETF` 占位名称。

### 设计说明

- ETF 池/资产主表用于保存“有哪些 ETF、名称、分类、风险、是否启用”等主数据。
- 行情价格不会天然全量存在数据库，需要按持仓、目标组合、启用池或用户新增代码同步。
- 当前持仓录入优先保证真实持仓可保存：先补日线，日线没有再补最新报价，最后才让用户临时填现价。

### 验收要点

- 新增一个不在 ETF 池里的 ETF 时，系统会自动登记候选 ETF。
- 日线缺失时，系统会尝试用最新报价补现价。
- 报价失败时仍能手动填写现价，不阻断持仓录入。

## v0.32.3-holding-refresh-combined - 2026-07-11

阶段：持仓刷新按钮合并

### 完成内容

- 当前持仓页移除单独的“刷新列表”和“刷新现价”按钮。
- 新增统一的“刷新持仓”按钮。
- 点击“刷新持仓”时，系统会先尝试补全/同步当前持仓现价，再从数据库重新读取最新持仓列表。

### 验收要点

- 用户不需要区分“刷新列表”和“刷新现价”。
- 没有持仓时，“刷新持仓”只重新加载列表。
- 有持仓时，“刷新持仓”会完成行情刷新和列表刷新。

## v0.32.2-holding-auto-save - 2026-07-11

阶段：持仓自动保存体验优化

### 完成内容

- 当前持仓新增、编辑、补仓确认后自动保存到数据库。
- 移除手动保存快照按钮，避免用户误以为弹窗确认后还需要二次保存。
- 新增“刷新列表”按钮，用于从数据库重新拉取最新持仓快照。

### 验收要点

- 新增持仓点击“确定并保存”后，持仓列表和数据库同步更新。
- 编辑或补仓确认后立即入库。
- 页面只保留刷新列表、刷新现价、运行持仓分析等必要操作。

## v0.32.1-holding-analysis-guard - 2026-07-11

阶段：持仓分析错误提示修复

### 完成内容

- 持仓分析接口捕获“没有目标组合”的业务错误，不再返回 500。
- 没有可用目标组合时返回中文提示：先运行目标组合或全流程，再做持仓分析。
- 前端运行 ID 默认留空，留空时后端自动使用最近一次目标组合；只有需要指定某次策略运行时才填写。

### 验收要点

- 没有目标组合时，点击“运行持仓分析”显示可理解的业务提示。
- 有最近目标组合时，不填写运行 ID 也可以直接分析。

## v0.32.0-holding-auto-sync - 2026-07-11

阶段：持仓录入自动补全

### 完成内容

- 当前持仓解析接口新增 `auto_sync` 和 `source` 参数。
- 输入不在 ETF 池里的持仓代码时，系统会自动登记为候选 ETF，不再阻止录入真实持仓。
- 如果缺少 clean 行情，系统会自动同步最近 45 天行情并重新返回现价。
- 前端“新增持仓/刷新现价”默认触发自动补全和行情同步。
- 自动同步仍失败时，前端允许用户临时填写现价并保存持仓快照，后续行情同步成功后可覆盖。
- 支持输入 `513050.SH`、`159928.SZ` 这类带交易所后缀的代码，后端会标准化为 6 位代码。

### 变更文件

- `backend/app/api/portfolio_api.py`
- `backend/app/schemas/portfolio_schema.py`
- `backend/app/services/holding_service.py`
- `backend/tests/test_holding_service.py`
- `frontend/src/api/client.ts`
- `frontend/src/views/Holdings.vue`
- `docs/04_API接口文档.md`
- `docs/10_系统流程说明.md`
- `README.md`

### 验收要点

- 当前持仓新增弹窗输入新 ETF 代码后，点击“自动补全/同步”会尝试自动登记资产并补行情。
- 不在精选 ETF 池中的真实持仓也可以录入，不再要求用户先去 ETF 池手动导入。
- 行情源失败时，可以临时填写现价保存快照，避免持仓录入流程被卡死。

## v0.31.0-smart-market-sync - 2026-07-11

阶段：核心 ETF 分层同步

### 完成内容

- 行情同步新增 `sync_scope` 参数，支持 `core`、`positions`、`target`、`plans`、`enabled`、`all`。
- 新增 `GET /api/market/sync-plan`，用于预览本次同步会覆盖哪些 ETF、哪些缺少 clean 行情价格。
- `core` 核心池按当前持仓、最新目标组合、定投建议涉及 ETF、启用 ETF 池去重合并，日常同步不再默认依赖全市场 ETF。
- 数据健康页新增同步范围选择和同步计划预览，手动填写 ETF 代码时仍优先按手动代码同步。
- 当前持仓解析中，`equity`/`stock` 底层资产类别统一显示为 ETF，避免 513050 等 ETF 被误显示为股票。
- 新增 `scripts/run_core_market_sync.py`，方便服务器后续通过 cron 或 systemd timer 做每日核心池增量同步。

### 变更文件

- `backend/app/services/market_service.py`
- `backend/app/api/market_api.py`
- `backend/app/schemas/market_schema.py`
- `backend/app/services/holding_service.py`
- `frontend/src/api/client.ts`
- `frontend/src/views/DataHealth.vue`
- `scripts/run_core_market_sync.py`
- `docs/04_API接口文档.md`
- `docs/10_系统流程说明.md`
- `docs/12_ETF功能优化路线图.md`
- `README.md`

### 验收要点

- 前端数据健康页默认选择核心池，并显示计划 ETF 数量、缺行情数量和最新行情日。
- `POST /api/market/sync` 在不传 `symbols` 时可按 `sync_scope=core` 自动选择 ETF。
- `GET /api/market/sync-plan?sync_scope=core` 可返回同步计划。
- 当前持仓输入 ETF 代码后，类型显示为 ETF；若缺少行情，提示用户先同步或临时补充现价。

## v0.1.0-phase1 - 2026-07-08

阶段：基础工程与数据库

### 完成内容

- 创建 FastAPI 后端项目结构。
- 增加 SQLAlchemy 数据库连接。
- 增加环境变量配置 `.env.example`。
- 增加 Dockerfile 和 `docker-compose.yml`。
- PostgreSQL 宿主机端口使用 `5433`，避免和本机已有 PostgreSQL 的 `5432` 冲突。
- Redis 宿主机端口使用 `6380`，避免和本机已有 Redis 的 `6379` 冲突。
- 增加 PostgreSQL 初始化 SQL。
- 创建需求文档中第一阶段要求的 21 张表。
- 初始化 ETF 资产池、默认策略配置、基础风控规则。
- 增加 `GET /health` 健康检查接口。
- 增加 `GET /api/assets` ETF 主数据查询接口。
- 增加项目文档体系。

### 新增文件

- `.gitignore`
- `.env.example`
- `README.md`
- `docker-compose.yml`
- `backend/Dockerfile`
- `backend/requirements.txt`
- `backend/app/main.py`
- `backend/app/core/*`
- `backend/app/models/*`
- `backend/app/schemas/asset_schema.py`
- `backend/app/services/asset_service.py`
- `backend/app/api/health_api.py`
- `backend/app/api/asset_api.py`
- `sql/init_schema.sql`
- `sql/seed_assets.sql`
- `docs/01_项目说明.md`
- `docs/02_系统架构.md`
- `docs/03_数据库设计.md`
- `docs/04_API接口文档.md`
- `docs/05_策略设计.md`
- `docs/06_风控设计.md`
- `docs/07_回测设计.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`

### 数据库变化

新增表：

- `asset_master`
- `market_data_raw`
- `market_data_clean`
- `etf_nav_premium`
- `trading_calendar`
- `data_quality_log`
- `factor_daily`
- `strategy_config`
- `strategy_run`
- `alpha_signal`
- `target_portfolio`
- `portfolio_position`
- `risk_rule`
- `risk_check_result`
- `rebalance_order`
- `backtest_run`
- `backtest_equity_curve`
- `backtest_trade`
- `backtest_metrics`
- `performance_attribution`
- `report_log`

新增种子数据：

- 10 只 ETF 初始资产。
- 1 个默认策略配置。
- 5 条基础风控规则。

### API 变化

新增：

- `GET /health`
- `GET /api/assets`

### 验收标准

- FastAPI 能启动。
- PostgreSQL 能连接。
- 数据库表能创建。
- ETF 初始数据能插入。
- `/docs` 能打开。
- `/health` 能返回健康状态。
- `/api/assets` 能返回 ETF 资产列表。

### 后续待办

- 阶段 2：实现行情采集、raw 数据保存、clean 数据保存、数据质量日志。
- 增加 API 测试。
- 增加数据库迁移工具 Alembic。
- GitHub 远程仓库连接后推送版本提交。

## v0.2.0-phase2-data-platform - 2026-07-08

阶段：数据平台第一步

### 完成内容

- 新增 AKShare ETF 日线行情同步服务。
- 新增 raw 行情入库逻辑，写入 `market_data_raw`。
- 新增 clean 行情入库逻辑，写入 `market_data_clean`。
- 新增数据质量检查逻辑，检查空数据、零价格、零成交量、价格异常、单日异常涨跌。
- 新增质量日志写入 `data_quality_log`。
- 新增行情查询和数据质量 API。
- 同步更新系统架构、API 文档和部署说明。

### 新增文件

- `backend/app/api/market_api.py`
- `backend/app/api/data_quality_api.py`
- `backend/app/schemas/market_schema.py`
- `backend/app/schemas/data_quality_schema.py`
- `backend/app/services/market_service.py`
- `backend/app/services/data_quality_service.py`

### 修改文件

- `backend/app/main.py`
- `docs/02_系统架构.md`
- `docs/04_API接口文档.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`

### 数据库变化

未新增表。复用第一阶段已创建的：

- `market_data_raw`
- `market_data_clean`
- `data_quality_log`

### API 变化

新增：

- `POST /api/market/sync`
- `GET /api/market/raw`
- `GET /api/market/clean`
- `GET /api/market/bars/{symbol}`
- `POST /api/data-quality/check`
- `GET /api/data-quality/logs`
- `GET /api/data-quality/status`

### 验收标准

- 单只 ETF 可以触发行情同步。
- raw 行情可以入库。
- clean 行情可以入库。
- 数据质量日志可以查询。
- `/api/market/bars/{symbol}` 可以查询 clean 行情。

### 注意事项

- AKShare 上游接口可能存在临时网络失败；失败会以单个 ETF 的 `failed` 结果返回，并写入质量日志。
- 当前数据质量检查仍是基础规则，后续需要结合交易日历检查缺失交易日。

### 后续待办

- 增加交易日历同步。
- 增加缺失交易日检测。
- 增加批量同步任务和 APScheduler 定时任务。
- 阶段 3：因子计算和 ETF 综合评分。

## v0.2.1-phase2-data-reliability - 2026-07-08

阶段：数据平台可靠性增强

### 完成内容

- 为行情同步增加东方财富 K 线 HTTP 备用源。
- `source=akshare` 时，AKShare 失败会自动 fallback 到 `eastmoney`。
- 新增交易日历同步服务，写入 `trading_calendar`。
- 新增交易日历查询接口。
- 数据质量检查新增基于交易日历的 open day 缺失检查。
- 将 `requests` 显式加入后端依赖。

### 新增文件

- `backend/app/api/calendar_api.py`
- `backend/app/schemas/calendar_schema.py`
- `backend/app/services/calendar_service.py`

### 修改文件

- `backend/app/main.py`
- `backend/app/services/market_service.py`
- `backend/app/services/data_quality_service.py`
- `backend/app/schemas/market_schema.py`
- `backend/requirements.txt`
- `docs/02_系统架构.md`
- `docs/04_API接口文档.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`

### 数据库变化

未新增表。复用第一阶段已创建的：

- `trading_calendar`
- `market_data_raw`
- `market_data_clean`
- `data_quality_log`

### API 变化

新增：

- `POST /api/calendar/sync`
- `GET /api/calendar/trading-days`

增强：

- `POST /api/market/sync` 支持 AKShare 失败后 fallback 到东方财富。
- `POST /api/data-quality/check` 支持根据 `trading_calendar` 检查开市日缺失数据。

### 验收标准

- 可以同步交易日历。
- 可以查询交易日历。
- 可以显式使用 `source=eastmoney` 同步单只 ETF 行情。
- clean 行情缺失检查能识别交易日历中的开市日缺口。

### 后续待办

- 增加 pytest 自动化测试。
- 增加 APScheduler 定时同步。
- 阶段 3：因子计算和 ETF 综合评分。

## v0.2.2-phase2-tests-and-batch-control - 2026-07-08

阶段：数据平台收尾

### 完成内容

- 为行情同步增加批量控制参数：
  - `max_symbols`
  - `request_interval_seconds`
- 行情同步响应新增批量统计：
  - `requested_symbols`
  - `skipped_symbols`
  - `success_count`
  - `failed_count`
- 新增 pytest 单元测试，覆盖行情标准化、东方财富 secid、fallback、交易日历 fallback、交易日历查询辅助逻辑。
- Dockerfile 复制 `backend/tests`，支持在 API 容器内运行测试。

### 新增文件

- `backend/tests/test_market_service.py`
- `backend/tests/test_calendar_service.py`
- `backend/tests/test_data_quality_service.py`

### 修改文件

- `backend/app/services/market_service.py`
- `backend/app/schemas/market_schema.py`
- `backend/app/api/market_api.py`
- `backend/Dockerfile`
- `docs/04_API接口文档.md`
- `docs/09_开发日志.md`

### 数据库变化

无。

### API 变化

增强：

- `POST /api/market/sync` 支持 `max_symbols` 和 `request_interval_seconds`。
- `POST /api/market/sync` 响应新增成功、失败、跳过数量统计。

### 验收标准

- pytest 测试通过。
- 批量同步可以限制本次最多同步数量。
- 批量同步响应能展示成功、失败和跳过数量。

### 后续待办

- 阶段 3：因子计算和 ETF 综合评分。

## v0.3.0-phase3-factor-engine - 2026-07-08

阶段：因子引擎

### 完成内容

- 新增因子计算服务，基于 `market_data_clean` 生成 `factor_daily`。
- 实现均线、收益率、波动率、回撤、流动性因子。
- 实现趋势、动量、波动、回撤、流动性、溢价评分。
- 实现综合 `alpha_score`。
- 新增因子计算、单 ETF 因子查询、综合评分排名 API。
- 新增因子服务单元测试。

### 新增文件

- `backend/app/api/factor_api.py`
- `backend/app/schemas/factor_schema.py`
- `backend/app/services/factor_service.py`
- `backend/tests/test_factor_service.py`

### 修改文件

- `backend/app/main.py`
- `docs/04_API接口文档.md`
- `docs/05_策略设计.md`
- `docs/09_开发日志.md`

### 数据库变化

未新增表。复用第一阶段已创建的：

- `factor_daily`

### API 变化

新增：

- `POST /api/factors/calculate`
- `GET /api/factors/{symbol}`
- `GET /api/factors/ranking`

### 验收标准

- 可以基于 clean 行情计算因子。
- 因子结果可以写入 `factor_daily`。
- 可以查询单只 ETF 因子。
- 可以查询 ETF 综合评分排名。
- pytest 测试通过。

### 后续待办

- 接入 ETF 净值和溢价率，替换临时 premium_score。
- 阶段 4：策略配置读取、Alpha 排名固化、目标组合生成。

## v0.4.0-phase4-strategy-portfolio - 2026-07-08

阶段：策略与目标组合

### 完成内容

- 新增策略运行服务。
- 支持读取 `strategy_config` 中的启用策略。
- 基于 `factor_daily.alpha_score` 创建 `strategy_run`。
- 生成并保存 `alpha_signal`。
- 生成并保存 `target_portfolio`。
- 新增策略配置查询、策略运行、最新信号查询、最新目标组合查询 API。
- 新增目标组合构建单元测试。

### 新增文件

- `backend/app/api/strategy_api.py`
- `backend/app/api/portfolio_api.py`
- `backend/app/schemas/strategy_schema.py`
- `backend/app/schemas/portfolio_schema.py`
- `backend/app/services/strategy_service.py`
- `backend/tests/test_strategy_service.py`

### 修改文件

- `backend/app/main.py`
- `docs/04_API接口文档.md`
- `docs/05_策略设计.md`
- `docs/09_开发日志.md`

### 数据库变化

未新增表。复用第一阶段已创建的：

- `strategy_config`
- `strategy_run`
- `alpha_signal`
- `target_portfolio`

### API 变化

新增：

- `GET /api/strategies`
- `POST /api/strategies/run`
- `GET /api/strategies/latest-signals`
- `GET /api/portfolio/target`

### 验收标准

- 可以读取策略配置。
- 可以基于最新因子运行策略。
- 可以保存 Alpha 信号。
- 可以保存目标组合。
- 目标组合权重合计为 100%。
- pytest 测试通过。

### 后续待办

- 阶段 5：独立风控引擎和调仓单生成。

## v0.5.0-phase5-risk-rebalance - 2026-07-08

阶段：风控与调仓

### 完成内容

- 新增独立风控服务。
- 实现单一 ETF 最大仓位约束。
- 实现权益资产最大仓位约束。
- 实现防御资产最低仓位约束。
- 实现现金最低仓位约束。
- 风控结果写入 `risk_check_result`。
- 风控修正写回 `target_portfolio.final_target_weight`。
- 新增调仓单生成服务，写入 `rebalance_order`。
- 新增风控和调仓 API。
- 新增风控规则单元测试。

### 新增文件

- `backend/app/api/risk_api.py`
- `backend/app/api/rebalance_api.py`
- `backend/app/schemas/risk_schema.py`
- `backend/app/services/risk_service.py`
- `backend/tests/test_risk_service.py`

### 修改文件

- `backend/app/main.py`
- `docs/04_API接口文档.md`
- `docs/06_风控设计.md`
- `docs/09_开发日志.md`

### 数据库变化

未新增表。复用第一阶段已创建的：

- `risk_rule`
- `risk_check_result`
- `target_portfolio`
- `rebalance_order`

### API 变化

新增：

- `POST /api/risk/check`
- `GET /api/risk/results`
- `POST /api/rebalance/generate`
- `GET /api/rebalance/orders`

### 验收标准

- 可以对指定 `run_id` 执行风控检查。
- 风控检查结果可查询。
- `target_portfolio.final_target_weight` 可被风控修正。
- 可以生成调仓建议单。
- pytest 测试通过。

### 后续待办

- 接入真实当前持仓 `portfolio_position`。
- 支持用户录入组合市值和当前持仓。
- 阶段 6：历史回测系统。

## v0.6.0-phase6-basic-backtest - 2026-07-08

阶段：基础回测系统

### 完成内容

- 新增基础回测服务。
- 实现等权买入持有基线回测。
- 读取 `market_data_clean` 生成回测净值曲线。
- 写入 `backtest_run`、`backtest_equity_curve`、`backtest_trade`、`backtest_metrics`。
- 新增回测运行、回测列表、净值曲线、交易记录、绩效指标 API。
- 新增基础回测单元测试。

### 新增文件

- `backend/app/api/backtest_api.py`
- `backend/app/schemas/backtest_schema.py`
- `backend/app/services/backtest_service.py`
- `backend/tests/test_backtest_service.py`

### 修改文件

- `backend/app/main.py`
- `docs/04_API接口文档.md`
- `docs/07_回测设计.md`
- `docs/09_开发日志.md`

### 数据库变化

未新增表。复用第一阶段已创建的：

- `backtest_run`
- `backtest_equity_curve`
- `backtest_trade`
- `backtest_metrics`

### API 变化

新增：

- `POST /api/backtest/run`
- `GET /api/backtest/runs`
- `GET /api/backtest/{backtest_id}`
- `GET /api/backtest/{backtest_id}/equity-curve`
- `GET /api/backtest/{backtest_id}/trades`
- `GET /api/backtest/{backtest_id}/metrics`

### 验收标准

- 可以基于 clean 行情运行等权买入持有回测。
- 可以查询净值曲线、交易记录和指标。
- pytest 测试通过。

### 后续待办

- 将阶段 4/5 的策略、风控、调仓逻辑接入月度回测。
- 增加基准对比和收益归因。

## v0.7.0-phase7-monthly-report - 2026-07-08

阶段：报告系统

### 完成内容

- 新增月度调仓报告生成服务。
- 报告以 Markdown 形式保存到 `report_log.content_markdown`。
- 报告汇总策略运行、Alpha 排名、目标组合、风控结果、调仓单和最近回测摘要。
- 新增报告生成和查询 API。
- 新增报告 Markdown 结构测试。

### 新增文件

- `backend/app/api/report_api.py`
- `backend/app/schemas/report_schema.py`
- `backend/app/services/report_service.py`
- `backend/tests/test_report_service.py`

### 修改文件

- `backend/app/main.py`
- `docs/02_系统架构.md`
- `docs/04_API接口文档.md`
- `docs/09_开发日志.md`

### 数据库变化

未新增表。复用第一阶段已创建的：

- `report_log`

### API 变化

新增：

- `POST /api/reports/monthly`
- `GET /api/reports`
- `GET /api/reports/{report_id}`

### 验收标准

- 可以基于最近一次或指定 `run_id` 生成月度报告。
- 报告内容保存为 Markdown。
- 可以查询报告列表和报告详情。
- pytest 测试通过。

### 后续待办

- 增加收益归因 `performance_attribution`。
- 增加前端报告展示页。

## v0.8.0-phase8-web-console - 2026-07-08

阶段：基础 Web 控制台

### 完成内容

- 新增 Vue 3 + Vite + Element Plus 前端项目。
- 新增左侧导航和控制台布局。
- 新增 Dashboard、ETF 池、数据健康、因子排名、目标组合、风控调仓、回测、报告页面。
- 接入现有后端 API。
- 使用 ECharts 展示 Alpha 排名、目标组合和回测净值曲线。
- 新增前端 Dockerfile，并在 `docker-compose.yml` 中增加 frontend 服务。

### 新增文件

- `frontend/package.json`
- `frontend/index.html`
- `frontend/tsconfig.json`
- `frontend/vite.config.ts`
- `frontend/Dockerfile`
- `frontend/src/*`

### 修改文件

- `docker-compose.yml`
- `README.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`

### API 变化

无。

### 验收标准

- 前端可以本地启动。
- 前端可以访问后端 API。
- 核心页面可以展示已有数据。
- 前端可以完成生产构建。

### 后续待办

- 增加前端操作按钮，例如同步行情、计算因子、运行策略、生成报告。
- 增加更完整的 Markdown 渲染。
- 增加响应式移动端导航。

## v0.9.0-phase9-operable-web-console - 2026-07-09

阶段：可操作 Web 控制台

### 完成内容

- 将前端主要导航、页面标题、表格字段和操作提示改为中文。
- 在数据健康页新增交易日历同步、行情同步、缺失数据检查和一键更新检查入口。
- 在因子排名页新增按日期范围计算因子的入口。
- 在目标组合页新增运行策略入口，支持填写策略代码和运行日期。
- 在风控调仓页新增执行风控检查和生成调仓单入口。
- 在回测页新增回测表单，可填写日期范围、ETF 代码和初始资金。
- 在报告页新增月度报告生成入口。
- 扩展前端 API client，封装现有后端 POST 操作接口。

### 修改文件

- `frontend/package.json`
- `frontend/src/api/client.ts`
- `frontend/src/App.vue`
- `frontend/src/router/index.ts`
- `frontend/src/styles.css`
- `frontend/src/views/*`
- `README.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`

### API 变化

无新增后端 API。本阶段复用已有接口：

- `POST /api/calendar/sync`
- `POST /api/market/sync`
- `POST /api/data-quality/check`
- `POST /api/factors/calculate`
- `POST /api/strategies/run`
- `POST /api/risk/check`
- `POST /api/rebalance/generate`
- `POST /api/backtest/run`
- `POST /api/reports/monthly`

### 验收标准

- 前端可以完成生产构建。
- 页面主要内容已中文化。
- 用户可以在前端页面内触发核心投研流程。
- 执行操作后页面会自动刷新相关表格或图表。

### 后续待办

- 增加全流程向导，把同步数据、计算因子、运行策略、风控调仓和生成报告串成一步式流程。
- 增加操作任务日志和更友好的错误详情展示。
- 对 ECharts 和 Element Plus 做代码分包，降低生产包体积。

## v0.10.0-phase10-workflow-wizard - 2026-07-09

阶段：全流程向导

### 完成内容

- 新增“全流程”页面，并加入左侧导航。
- 支持在一个页面中按顺序执行：同步交易日历、同步 ETF 行情、检查数据质量、计算因子、运行策略、执行风控、生成调仓单、生成月度报告。
- 新增流程参数表单，可配置日期范围、ETF 代码、最大同步数量、策略代码、组合市值和是否生成报告。
- 新增纵向步骤条，展示每一步的等待、执行、完成、失败和跳过状态。
- 新增执行日志表，记录每一步的执行时间、状态和关键返回结果。
- 对策略返回的 `run_id` 增加前端校验，避免后续风控和调仓使用无效运行编号。

### 新增文件

- `frontend/src/views/Workflow.vue`

### 修改文件

- `frontend/src/App.vue`
- `frontend/src/router/index.ts`
- `frontend/src/styles.css`
- `frontend/package.json`
- `frontend/package-lock.json`
- `README.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`

### API 变化

无新增后端 API。本阶段复用已有操作接口。

### 验收标准

- 前端可以完成生产构建。
- Web 控制台出现“全流程”导航入口。
- 流程执行时每一步都有状态反馈和日志记录。
- 获取到有效 `run_id` 后才继续执行风控、调仓和报告生成。

### 后续待办

- 增加后端异步任务队列，避免长流程阻塞 HTTP 请求。
- 增加流程运行历史表，持久化每次全流程执行结果。
- 增加失败步骤重试按钮。

## v0.11.0-phase11-async-workflow - 2026-07-09

阶段：后端异步任务

### 完成内容

- 新增后端工作流任务模型 `workflow_task` 和 `workflow_task_step`。
- 新增异步工作流 API：创建任务、查询任务详情、查询任务列表。
- 使用 FastAPI `BackgroundTasks` 在后端后台执行完整流程，避免前端长时间阻塞等待。
- 后端执行过程中逐步写入任务状态、当前步骤、步骤结果和错误信息。
- 前端“全流程”页面改为创建后台任务后轮询任务进度。
- 前端步骤条和日志表改为读取后端持久化状态。
- 现有数据库可通过工作流 API 自动创建任务表；新环境也已更新 `sql/init_schema.sql`。

### 新增文件

- `backend/app/models/workflow.py`
- `backend/app/schemas/workflow_schema.py`
- `backend/app/services/workflow_service.py`
- `backend/app/api/workflow_api.py`
- `backend/tests/test_workflow_service.py`

### 修改文件

- `backend/app/main.py`
- `backend/app/models/__init__.py`
- `sql/init_schema.sql`
- `frontend/src/api/client.ts`
- `frontend/src/views/Workflow.vue`
- `frontend/src/styles.css`
- `frontend/package.json`
- `frontend/package-lock.json`
- `README.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`

### API 变化

新增：

- `POST /api/workflows/run`
- `GET /api/workflows`
- `GET /api/workflows/{task_id}`

### 验收标准

- 前端可以创建后台工作流任务。
- 后端能持久化任务与步骤状态。
- 前端可以轮询查看任务进度和执行日志。
- 前端生产构建通过。
- 后端代码编译通过。

### 后续待办

- 将进程内后台任务升级为 Redis/Celery 或 APScheduler 持久队列。
- 增加失败步骤重试 API。
- 增加任务取消 API。

## v0.12.0-phase12-workflow-controls - 2026-07-09

阶段：任务控制与重试

### 完成内容

- 后端新增任务取消能力，支持将执行中或等待中的任务标记为 `cancelled`。
- 后端新增失败步骤重试能力，可从失败步骤开始重置后续未完成步骤并重新执行。
- 工作流任务状态新增 `partial_success` 和 `cancelled`。
- 全流程页面新增最近任务列表，可选择历史任务查看详情。
- 全流程页面新增取消任务、重试失败步骤和刷新按钮。
- 前端任务日志、状态标签和步骤条适配部分完成、取消和重试场景。
- 重写工作流服务与前端页面，修复中文显示内容并保持 UTF-8 文本。

### 修改文件

- `backend/app/services/workflow_service.py`
- `backend/app/api/workflow_api.py`
- `backend/tests/test_workflow_service.py`
- `frontend/src/api/client.ts`
- `frontend/src/views/Workflow.vue`
- `frontend/package.json`
- `frontend/package-lock.json`
- `README.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`

### API 变化

新增：

- `POST /api/workflows/{task_id}/cancel`
- `POST /api/workflows/{task_id}/retry-failed`

### 验收标准

- 前端可以查看最近 20 个任务。
- 执行中的任务可以发起取消请求。
- 失败任务可以从失败步骤开始重试。
- 前端生产构建通过。
- 后端测试通过。

### 后续待办

- 增加任务取消的强制中断机制，目前取消会在步骤之间生效。
- 增加按任务状态筛选。
- 增加任务详情导出。

## v0.12.1-layout-fix - 2026-07-09

阶段：前端布局修复

### 修复内容

- 修复全流程页面中 `.span-5` 和 `.span-7` 未定义导致面板被 CSS Grid 压缩成窄列的问题。
- 最近任务、任务日志等面板在中等宽度下自动改为整行显示，避免表格标题和内容被挤成竖排。
- 移动端断点补充 `.span-5` 和 `.span-7`，保证所有栅格类行为一致。

### 修改文件

- `frontend/src/styles.css`
- `frontend/package.json`
- `frontend/package-lock.json`
- `docs/09_开发日志.md`

### 验收标准

- 前端生产构建通过。
- `/workflow` 页面可以正常访问。
- 最近任务和任务日志面板不再被压缩成异常窄列。

## v0.12.2-docs-and-report-cn - 2026-07-09

阶段：文档与报告体验优化

### 完成内容

- 新增 `docs/10_系统流程说明.md`，从业务流程角度说明平台目标、数据流、前端页面、后端模块、关键数据表、全流程任务、策略修改、真实行情 API 接入、报告生成和排查路径。
- 修复前端滚动时左侧菜单跟随页面滑走的问题，改为左侧菜单固定，主内容区域独立滚动。
- 后端月度报告改为中文为主，并保留必要英文术语，例如 `Monthly Rebalance Report`、`Alpha Ranking`、`Target Portfolio`、`Risk Check`、`Rebalance Orders`。
- 前端报告页增强 Markdown 渲染，支持把 Markdown 表格渲染为 HTML 表格，并增加基础转义，避免直接插入未转义文本。
- 优化报告页表格样式，支持横向滚动，减少内容挤压。

### 新增文件

- `docs/10_系统流程说明.md`

### 修改文件

- `README.md`
- `backend/app/services/report_service.py`
- `frontend/src/styles.css`
- `frontend/src/views/Reports.vue`
- `docs/09_开发日志.md`

### 验收标准

- 左侧菜单在桌面端滚动时保持固定。
- 新生成的报告以中文为主，保留必要英文术语。
- 报告页 Markdown 表格可以正常渲染。
- 前端生产构建通过。
- 后端测试通过。

## v0.13.0-holdings-analysis - 2026-07-09

阶段：当前持仓与持仓分析

### 完成内容

- 新增当前持仓快照能力，用户可以录入 ETF 代码、份额、当前市值和持仓成本。
- 后端按持仓快照总市值自动计算每只 ETF 的当前权重。
- 新增持仓分析结果表，把当前权重与目标组合权重对比，输出加仓、减仓、持有、减仓/退出建议。
- 调仓建议单开始读取当前持仓权重，不再始终按当前权重为 0 计算。
- 新增前端“当前持仓”页面，支持保存持仓快照、查看当前权重、运行持仓分析。
- 持仓分析原因以中文为主，保留 `alpha_score` 等必要英文指标名。

### 新增接口

- `POST /api/portfolio/positions`
- `GET /api/portfolio/positions`
- `POST /api/portfolio/holdings/analyze`
- `GET /api/portfolio/holdings/analysis`

### 新增文件

- `backend/app/services/holding_service.py`
- `backend/tests/test_holding_service.py`
- `frontend/src/views/Holdings.vue`

### 修改文件

- `README.md`
- `backend/app/api/portfolio_api.py`
- `backend/app/models/portfolio.py`
- `backend/app/models/__init__.py`
- `backend/app/schemas/portfolio_schema.py`
- `backend/app/services/risk_service.py`
- `backend/tests/test_report_service.py`
- `frontend/src/App.vue`
- `frontend/src/api/client.ts`
- `frontend/src/router/index.ts`
- `frontend/package.json`
- `frontend/package-lock.json`
- `sql/init_schema.sql`
- `docs/03_数据库设计.md`
- `docs/04_API接口文档.md`
- `docs/09_开发日志.md`
- `docs/10_系统流程说明.md`

### 验收标准

- 前端生产构建通过。
- 后端测试通过。
- 可以保存当前持仓快照。
- 可以基于某个 `run_id` 生成持仓分析结果。
- 生成调仓建议单时会优先读取当前持仓权重。

## v0.13.1-docs-polish - 2026-07-09

阶段：文档包装与交接优化

### 完成内容

- 新增 `docs/00_文档导航.md`，作为项目文档地图，说明阅读顺序、最小 AI 上下文包和版本维护规则。
- 重写 `README.md`，补充当前版本、主要功能、快速启动和核心文档入口。
- 重写 `docs/01_项目说明.md`，从当前 v0.13 能力重新说明项目定位、边界、已实现能力、未实现能力和使用路径。
- 重写 `docs/02_系统架构.md`，补充前端结构、当前 API 文件、核心 service、持仓分析流和完整业务数据流。
- 优化 `docs/08_部署说明.md`，补充当前持仓页面说明、前端缓存排查和持仓分析无结果排查。

### 新增文件

- `docs/00_文档导航.md`

### 修改文件

- `README.md`
- `docs/01_项目说明.md`
- `docs/02_系统架构.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`

### 验收标准

- 文档入口能明确告诉维护者先读什么。
- 过期的 v0.1 阶段描述已更新为当前版本状态。
- 后续把文档交给 AI 时，可以快速定位项目结构、业务流程、接口和维护规范。

## v0.14.0-investment-plan - 2026-07-09

阶段：定投计划与本期投入建议

### 完成内容

- 新增定投计划表 `investment_plan`，保存计划名称、开始日期、定投期数、每期金额、总预算和备注。
- 新增定投建议表 `investment_plan_suggestion`，保存每一期对 ETF 的建议投入金额和原因。
- 新增后端定投服务，根据目标组合和当前持仓权重缺口分配本期定投资金。
- 当前算法优先把资金分给目标权重大于当前权重的低配 ETF；如果没有低配 ETF，则按目标权重分配。
- 新增前端“定投计划”页面，支持创建计划、选择计划、生成本期建议和查看建议列表。
- 更新项目版本、API 文档、数据库文档、系统流程说明和部署排查说明。

### 新增接口

- `POST /api/portfolio/investment-plans`
- `GET /api/portfolio/investment-plans`
- `POST /api/portfolio/investment-plans/{plan_id}/analyze`
- `GET /api/portfolio/investment-plans/{plan_id}/suggestions`

### 新增文件

- `backend/app/services/investment_plan_service.py`
- `backend/tests/test_investment_plan_service.py`
- `frontend/src/views/InvestmentPlans.vue`

### 修改文件

- `README.md`
- `backend/app/main.py`
- `backend/app/api/portfolio_api.py`
- `backend/app/models/portfolio.py`
- `backend/app/models/__init__.py`
- `backend/app/schemas/portfolio_schema.py`
- `frontend/src/App.vue`
- `frontend/src/api/client.ts`
- `frontend/src/router/index.ts`
- `frontend/package.json`
- `frontend/package-lock.json`
- `sql/init_schema.sql`
- `docs/00_文档导航.md`
- `docs/01_项目说明.md`
- `docs/02_系统架构.md`
- `docs/03_数据库设计.md`
- `docs/04_API接口文档.md`
- `docs/05_策略设计.md`
- `docs/06_风控设计.md`
- `docs/07_回测设计.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`
- `docs/10_系统流程说明.md`

### 验收标准

- 后端测试通过。
- 前端生产构建通过。
- 可以创建定投计划。
- 可以基于目标组合和当前持仓生成本期定投建议。
- 建议结果只用于分析，不触发真实资金操作。

## v0.15.0-portfolio-workbench - 2026-07-09

阶段：组合工作台与决策链整合

### 完成内容

- 新增前端“组合工作台”页面，把目标组合、当前持仓、持仓偏离、定投建议和调仓建议集中展示。
- 新增组合层面指标，包括当前持仓市值、目标 ETF 数、低配资产数、高配资产数。
- 新增偏离解释，按当前权重和目标权重判断低配、高配、接近目标或考虑退出。
- 新增组合提示卡片，展示偏离最大 ETF、本期定投优先 ETF 和当前调仓动作数量。
- 左侧导航新增“组合工作台”，便于从一个页面理解完整投资决策链。
- 前端 `RebalanceOrder` 类型补充 `reason` 字段，用于展示调仓说明。
- 更新项目版本号和核心文档说明。

### 新增文件

- `frontend/src/views/PortfolioWorkbench.vue`

### 修改文件

- `README.md`
- `backend/app/main.py`
- `frontend/src/App.vue`
- `frontend/src/api/client.ts`
- `frontend/src/router/index.ts`
- `frontend/src/styles.css`
- `frontend/package.json`
- `frontend/package-lock.json`
- `docs/01_项目说明.md`
- `docs/02_系统架构.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`
- `docs/10_系统流程说明.md`

### 验收标准

- 前端生产构建通过。
- 页面 `/portfolio-workbench` 可以正常访问。
- 页面能展示当前持仓、目标权重、偏离状态、定投建议和调仓建议。
- 组合工作台只做分析展示，不触发真实资金操作。

## v0.16.0-strategy-backtest-rebalance - 2026-07-09

阶段：策略月度调仓回测

### 完成内容

- 新增回测模式 `core_etf_rotation_monthly`。
- 策略月度调仓回测会在每月第一个可交易日读取最近 Alpha 排名，复用目标权重构造规则，并按目标权重调仓。
- 回测支持 `monthly_contribution`，月份切换时把每月追加资金加入现金并参与后续调仓。
- 回测交易记录会写入 `backtest_trade`，包括买入、卖出、价格、数量、金额、手续费、滑点和原因。
- 回测指标中的累计收益率开始支持以总投入本金为分母，避免定投资金导致收益率失真。
- 前端回测页新增回测模式选择、每月追加资金、手续费率、滑点率和交易记录表格。
- 更新版本号、API 文档、回测设计、系统流程和部署说明。

### 修改文件

- `README.md`
- `backend/app/main.py`
- `backend/app/services/backtest_service.py`
- `backend/tests/test_backtest_service.py`
- `frontend/src/api/client.ts`
- `frontend/src/views/Backtest.vue`
- `frontend/package.json`
- `frontend/package-lock.json`
- `docs/01_项目说明.md`
- `docs/02_系统架构.md`
- `docs/04_API接口文档.md`
- `docs/07_回测设计.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`
- `docs/10_系统流程说明.md`

### 验收标准

- 前端生产构建通过。
- 后端测试通过。
- API 可以运行 `core_etf_rotation_monthly` 回测。
- 可以查询策略调仓回测的净值曲线、指标和交易记录。
- 回测只做历史分析，不触发真实交易。

## v0.17.0-factor-research-lab - 2026-07-09

阶段：因子研究实验室

### 完成内容

- 新增后端因子研究服务 `factor_research_service.py`。
- 新增 `POST /api/factors/research` 接口，支持指定日期范围、前瞻天数和分组数量。
- 计算每个因子的 IC、Rank IC、IC 胜率和有效性判断。
- 计算因子两两相关性矩阵。
- 计算按因子值分组后的平均未来收益。
- Rank IC 使用 rank 后的 Pearson 相关，不额外引入 scipy 依赖。
- 新增前端“因子研究”页面，展示样本数、因子数、有效因子数、IC 表、相关性表和分组收益表。
- 左侧导航新增“因子研究”入口。
- 更新版本号、API 文档、策略设计、系统流程和部署说明。

### 新增接口

- `POST /api/factors/research`

### 新增文件

- `backend/app/services/factor_research_service.py`
- `backend/tests/test_factor_research_service.py`
- `frontend/src/views/FactorResearch.vue`

### 修改文件

- `README.md`
- `backend/app/main.py`
- `backend/app/api/factor_api.py`
- `backend/app/schemas/factor_schema.py`
- `frontend/src/App.vue`
- `frontend/src/api/client.ts`
- `frontend/src/router/index.ts`
- `frontend/package.json`
- `frontend/package-lock.json`
- `docs/01_项目说明.md`
- `docs/02_系统架构.md`
- `docs/04_API接口文档.md`
- `docs/05_策略设计.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`
- `docs/10_系统流程说明.md`

### 验收标准

- 前端生产构建通过。
- 后端测试通过。
- API 可以返回因子研究结果。
- 前端 `/factor-research` 页面可以正常访问。
- 因子研究只做历史统计分析，不代表未来收益。

## v0.18.0-tushare-source-ready - 2026-07-09

阶段：真实数据源接入准备

### 完成内容

- 新增 `backend/app/services/tushare_service.py`，封装 Tushare HTTP API 调用。
- 新增 Tushare 配置项：`TUSHARE_TOKEN`、`TUSHARE_REQUEST_INTERVAL_SECONDS`。
- 交易日历同步支持 `source=tushare`，调用 `trade_cal` 接口。
- 行情同步支持 `source=tushare`，按 ETF 代码和交易所映射 `ts_code`，调用 `fund_daily`。
- 对 Tushare 权限不足、空数据和接口错误返回更明确的失败信息。
- `source=tushare` 且未显式传间隔时，自动使用系统配置中的节流间隔。
- 更新 README、API 文档、部署说明和系统流程说明，补充真实数据源接入方法。

### 新增文件

- `backend/app/services/tushare_service.py`

### 修改文件

- `README.md`
- `docker-compose.yml`
- `backend/app/api/calendar_api.py`
- `backend/app/core/config.py`
- `backend/app/main.py`
- `backend/app/schemas/calendar_schema.py`
- `backend/app/schemas/market_schema.py`
- `backend/app/services/calendar_service.py`
- `backend/app/services/market_service.py`
- `docs/04_API接口文档.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`
- `docs/10_系统流程说明.md`

### API 变化

增强：

- `POST /api/calendar/sync` 支持 `source=tushare`
- `POST /api/market/sync` 支持 `source=tushare`
- `POST /api/market/sync` 响应新增 `request_interval_seconds`

### 验收标准

- 后端代码编译通过。
- API 可以识别 `source=tushare`。
- 容器可以读取 `TUSHARE_*` 环境变量。
- 真接口验证时能够在低请求量模式下运行。

### 注意事项

- 当前共享账号应优先小批量、串行调用，避免频繁批量同步。
- `trade_cal` 与 `fund_daily` 的权限等级可能不同，交易日历成功不代表 ETF 日线一定有权限。

## v0.18.1-tushare-frontend-controls - 2026-07-09

阶段：真实数据源前端可操作化

### 完成内容

- 前端“数据健康”页面新增日历源、行情源和请求间隔设置。
- 前端“数据健康”页面新增数据源使用提示，帮助区分共享 Tushare、AKShare 和 Eastmoney 的适用场景。
- 前端“全流程”页面新增 `calendar_source`、`market_source`、`request_interval_seconds` 参数。
- 后端工作流请求模型支持保存并执行上述真实数据源参数。
- 工作流中的交易日历和行情步骤会按任务参数选择数据源，不再固定走默认源。
- 重新构建并验证前端与后端容器，确保页面已实际展示新控件。

### 修改文件

- `README.md`
- `backend/app/main.py`
- `backend/app/schemas/workflow_schema.py`
- `backend/app/services/workflow_service.py`
- `frontend/package.json`
- `frontend/src/api/client.ts`
- `frontend/src/styles.css`
- `frontend/src/views/DataHealth.vue`
- `frontend/src/views/Workflow.vue`
- `docs/04_API接口文档.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`

### API 变化

增强：

- `POST /api/workflows/run` 支持 `calendar_source`
- `POST /api/workflows/run` 支持 `market_source`
- `POST /api/workflows/run` 支持 `request_interval_seconds`

### 验收标准

- 前端生产构建通过。
- 后端测试通过。
- `/data-health` 页面可见数据源和请求间隔控件。
- `/workflow` 页面可见数据源和请求间隔控件。
- 后台流程能按所选真实数据源执行。

## v0.18.2-incremental-sync - 2026-07-09

阶段：增量同步与共享 token 节流

### 完成内容

- 后端交易日历同步新增 `incremental` 模式，会先读取当前市场已存最新交易日，只补后续缺口。
- 后端 ETF 行情同步新增 `incremental` 模式，会先读取每只 ETF 已存最新行情日期，只补后续缺口。
- 如果某个市场或 ETF 已经同步到目标结束日期，系统会直接跳过，不再重复请求外部接口。
- 后端工作流新增 `incremental_sync` 参数，后台全流程可直接按增量模式执行。
- 前端“数据健康”页面新增“增量同步”开关，并默认开启。
- 前端“全流程”页面新增“增量同步”开关，并默认开启。
- 增量提示文案与 Tushare 节流提示联动更新，便于共享账号使用。

### 修改文件

- `README.md`
- `backend/app/api/calendar_api.py`
- `backend/app/api/market_api.py`
- `backend/app/main.py`
- `backend/app/schemas/calendar_schema.py`
- `backend/app/schemas/market_schema.py`
- `backend/app/schemas/workflow_schema.py`
- `backend/app/services/calendar_service.py`
- `backend/app/services/market_service.py`
- `backend/app/services/workflow_service.py`
- `backend/tests/test_calendar_service.py`
- `backend/tests/test_market_service.py`
- `frontend/package.json`
- `frontend/package-lock.json`
- `frontend/src/api/client.ts`
- `frontend/src/views/DataHealth.vue`
- `frontend/src/views/Workflow.vue`
- `docs/04_API接口文档.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`

### API 变化

增强：

- `POST /api/calendar/sync` 支持 `incremental`
- `POST /api/market/sync` 支持 `incremental`
- `POST /api/workflows/run` 支持 `incremental_sync`

### 验收标准

- 后端编译通过。
- pytest 测试通过。
- 前端生产构建通过。
- “数据健康”页面可见“增量同步”开关。
- “全流程”页面可见“增量同步”开关。
- 增量模式下，已同步到最新日期的市场或 ETF 不会重复请求外部接口。

## v0.18.3-asset-pool-expansion - 2026-07-09

阶段：ETF 主数据池扩展与维护

### 完成内容

- 扩展初始化 `asset_master` 样本池，补充更多常见宽基、行业、跨境和防御类 ETF。
- 后端新增 `POST /api/assets/batch-upsert`，支持批量新增或更新 ETF 主数据。
- 前端“ETF 池”页面从只读表升级为主数据维护入口。
- 前端新增“导入扩展示例池”按钮，可快速补充更多常见 ETF。
- 前端新增批量导入文本框，支持按行导入自己的 ETF 清单。
- 文档明确说明：当前内置 ETF 池是起步样本池，不是全市场全量清单。

### 修改文件

- `README.md`
- `backend/app/api/asset_api.py`
- `backend/app/main.py`
- `backend/app/schemas/asset_schema.py`
- `backend/app/services/asset_service.py`
- `frontend/package.json`
- `frontend/package-lock.json`
- `frontend/src/api/client.ts`
- `frontend/src/views/Assets.vue`
- `sql/seed_assets.sql`
- `docs/03_数据库设计.md`
- `docs/04_API接口文档.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`

### API 变化

新增：

- `POST /api/assets/batch-upsert`

### 验收标准

- 后端编译通过。
- pytest 测试通过。
- 前端生产构建通过。
- “ETF 池”页面支持导入扩展示例池。
- “ETF 池”页面支持批量导入自定义 ETF 主数据。

## v0.18.4-asset-pool-controls - 2026-07-09

阶段：ETF 研究池控制面板

### 完成内容

- 后端新增 `PATCH /api/assets/{symbol}`，支持单只 ETF 主数据更新。
- 前端“ETF 池”页面新增关键词筛选。
- 前端“ETF 池”页面新增按启用状态、资产类别、区域筛选。
- 前端“ETF 池”页面新增逐只启用 / 停用研究池开关。
- 前端“ETF 池”页面新增“复制已启用代码”，方便后续同步或策略样本选择。

### 修改文件

- `README.md`
- `backend/app/api/asset_api.py`
- `backend/app/main.py`
- `backend/app/schemas/asset_schema.py`
- `backend/app/services/asset_service.py`
- `frontend/package.json`
- `frontend/package-lock.json`
- `frontend/src/api/client.ts`
- `frontend/src/views/Assets.vue`
- `docs/04_API接口文档.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`

### API 变化

新增：

- `PATCH /api/assets/{symbol}`

### 验收标准

- 后端编译通过。
- pytest 测试通过。
- 前端生产构建通过。
- “ETF 池”页面支持启用 / 停用研究对象。
- “ETF 池”页面支持按状态、类别、区域筛选。

## v0.19.0-data-readiness-gate - 2026-07-10

阶段：服务器上线前的数据可信度门禁

### 完成内容

- 全流程默认开启严格数据门禁，行情同步全部失败或部分标的失败时不再继续生成策略建议。
- 新增最近交易日检查，使用已同步交易日历确认 clean 行情是否更新到目标日期。
- 新增最少历史样本检查，默认要求每只 ETF 在所选范围内至少有 200 条日线。
- 行情同步、质量检查和因子计算统一使用同一批 ETF，修复 `max_symbols` 只限制行情但不限制因子范围的问题。
- 因子计算全部失败或严格模式下部分失败时停止后续策略、风控、调仓和报告步骤。
- 门禁失败时保留批量执行结果、最新行情日期、样本数和具体失败原因，支持修复数据后重试失败步骤。
- 前端“全流程”页面新增“严格门禁”和“最少样本”控制项，并默认使用适合服务器运行的安全配置。

### API 变化

增强：

- `POST /api/workflows/run` 支持 `strict_data_validation`，默认 `true`。
- `POST /api/workflows/run` 支持 `minimum_history_bars`，默认 `200`。

### 修改文件

- `README.md`
- `backend/app/main.py`
- `backend/app/schemas/workflow_schema.py`
- `backend/app/services/workflow_service.py`
- `backend/tests/test_workflow_service.py`
- `frontend/package.json`
- `frontend/package-lock.json`
- `frontend/src/api/client.ts`
- `frontend/src/views/Workflow.vue`
- `docs/04_API接口文档.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`
- `docs/10_系统流程说明.md`

### 验收结果

- 后端代码编译通过。
- 后端测试 `42 passed`。
- 前端 TypeScript 检查和生产构建通过。
- 严格模式会拒绝全部失败和部分失败的批量结果。
- 非严格模式仍可用于人工排障，但正式服务器运行建议保持严格模式。

## v0.20.0-single-user-auth - 2026-07-10

阶段：单用户服务器远程访问保护

### 完成内容

- 新增单管理员认证接口：会话状态、登录和退出。
- 除健康检查和认证接口外，全部业务 API 统一接入认证依赖。
- 管理员密码使用 PBKDF2-SHA256 哈希，服务器不需要保存明文密码。
- 登录成功后使用 HMAC 签名的限时会话，并写入 HttpOnly、SameSite Cookie。
- 新增 15 分钟窗口内最多 5 次失败尝试的登录限速。
- 新增 `scripts/generate_auth_secrets.py`，交互生成密码哈希和随机会话密钥。
- 前端新增中文登录页、路由会话检查、401 跳转和退出按钮。
- 本地开发默认关闭鉴权，服务器通过 `AUTH_ENABLED=true` 强制开启。

### 新增接口

- `GET /api/auth/status`
- `POST /api/auth/login`
- `POST /api/auth/logout`

### 验收结果

- 后端代码编译通过。
- 后端测试 `46 passed`。
- 前端 TypeScript 检查和生产构建通过。
- 浏览器验证未登录跳转、正确登录、业务 API 访问和退出会话全部正常。

## v0.21.0-production-deployment - 2026-07-10

阶段：单用户服务器生产部署

### 完成内容

- 新增独立 `compose.production.yml`，与本地开发编排隔离。
- 生产环境只暴露 Caddy 的 80/443，数据库、Redis、API 和前端服务不映射公网端口。
- 新增 Caddy 自动 HTTPS、HTTP 跳转、安全响应头、压缩和访问日志配置。
- 前端 Nginx 新增静态资源缓存、上传大小限制、代理超时和转发来源头。
- 生产 API 新增 Host 白名单并关闭 Swagger、ReDoc 和 OpenAPI JSON。
- 新增生产配置启动校验，拒绝未开启鉴权、不安全 Cookie、默认数据库密码、占位密钥和通配 Host。
- 反向代理模式下登录限速使用真实客户端来源地址。
- 新增 `.env.production.example` 和完整服务器部署、更新、日志排查说明。

### 验收结果

- 生产 Compose 配置校验通过。
- Caddyfile 官方语法校验通过。
- API 与前端生产镜像构建通过。
- 后端测试 `52 passed`。
- 生产配置缺失关键安全项时会拒绝启动。

## v0.22.0-database-operations - 2026-07-10

阶段：服务器数据运维底座

### 完成内容

- 新增 Alembic 数据库迁移框架。
- 新增当前 SQLAlchemy ORM 模型基线迁移 `20260710_0001`。
- API Docker 镜像启动前自动执行 `alembic upgrade head`。
- 新增生产数据库备份脚本 `scripts/backup_database.py`。
- 新增生产数据库恢复脚本 `scripts/restore_database.py`。
- 备份脚本生成 PostgreSQL 自定义格式 `.dump` 文件和 `.sha256` 校验文件。
- 备份脚本会用 `pg_restore --list` 验证备份文件可读。
- 恢复脚本要求 `--confirm-database` 与运行中容器数据库名完全一致。
- 恢复脚本在恢复期间暂停 API，结束后重新启动 API。
- 部署文档补充更新前备份、恢复保护规则和迁移失败排查说明。

### 新增文件

- `backend/alembic.ini`
- `backend/alembic/env.py`
- `backend/alembic/script.py.mako`
- `backend/alembic/versions/20260710_0001_current_schema_baseline.py`
- `scripts/backup_database.py`
- `scripts/restore_database.py`

### 修改文件

- `.gitignore`
- `README.md`
- `backend/Dockerfile`
- `backend/app/main.py`
- `backend/requirements.txt`
- `frontend/package.json`
- `frontend/package-lock.json`
- `docs/02_系统架构.md`
- `docs/03_数据库设计.md`
- `docs/04_API接口文档.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`

### 验收标准

- 后端代码编译通过。
- Alembic 能升级到当前基线版本 `20260710_0001`。
- 备份脚本能生成可读备份和 SHA256 校验文件。
- 后端测试 `52 passed`。

## v0.23.0-durable-workflow-worker - 2026-07-10

阶段：服务器远程运行稳定性

### 完成内容

- 新增 `WORKFLOW_EXECUTION_MODE` 配置，本地默认 `inline`，生产默认 `worker`。
- 新增独立 `backend/app/worker.py`，负责轮询 pending 工作流任务并执行。
- 新增任务认领函数，worker 按创建时间领取最早的 pending 任务。
- 新增 worker 启动恢复逻辑，将异常中断的 running 任务恢复为 pending 并重试当前步骤。
- worker 写入 Redis 心跳，便于生产就绪检查。
- 新增 `/health/live` 和 `/health/ready`。
- 生产 API healthcheck 改为 `/health/ready`，会同时检查数据库和 worker 心跳。
- 生产 Compose 新增 `worker` 服务，前端和 Caddy 等待 API 与 worker 都健康后再对外提供服务。
- 部署文档补充 worker 日志、pending 任务排查和恢复说明。

### 新增文件

- `backend/app/worker.py`

### 修改文件

- `.env.example`
- `.env.production.example`
- `README.md`
- `backend/app/api/health_api.py`
- `backend/app/api/workflow_api.py`
- `backend/app/core/config.py`
- `backend/app/main.py`
- `backend/app/services/workflow_service.py`
- `backend/tests/test_config.py`
- `compose.production.yml`
- `docker-compose.yml`
- `frontend/package.json`
- `frontend/package-lock.json`
- `docs/02_系统架构.md`
- `docs/04_API接口文档.md`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`

### 验收标准

- 后端代码编译通过。
- 后端测试 `53 passed`。
- 前端生产构建通过。
- 生产 Compose 配置校验通过。

## v0.24.0-server-deploy-runbook - 2026-07-10

阶段：服务器上线辅助

### 完成内容

- 新增生产环境变量校验脚本，检查域名、邮箱、数据库密码、登录保护、worker 模式和 Tushare 节流配置。
- 新增服务器部署脚本，串联环境校验、代码更新、更新前备份、Compose 配置校验、构建启动和 API ready 检查。
- 新增服务器上线清单，记录需要用户提供的服务器信息、首次部署步骤、后续更新流程和常用排查命令。
- README 增加上线辅助能力和服务器上线清单入口。

### 新增文件

- `scripts/validate_production_env.py`
- `scripts/deploy_production.py`
- `docs/11_服务器上线清单.md`

### 修改文件

- `README.md`
- `docs/09_开发日志.md`

### 验收标准

- 新增脚本 Python 编译通过。
- 生产环境示例文件能被校验脚本正确识别为占位配置并拒绝。
- 生产部署脚本能输出帮助信息。

## v0.25.0-external-caddy-deployment - 2026-07-10

阶段：多项目服务器接入

### 完成内容

- 新增 `compose.production.external-caddy.yml`，适配服务器已有 Caddy/Nginx 占用 80/443 的场景。
- 外部 Caddy 模式不启动项目自带 Caddy，只把前端绑定到 `127.0.0.1:5174`。
- 保持 PostgreSQL、Redis、API 和 worker 不暴露公网端口。
- 部署说明和服务器上线清单补充现有 Caddy 反向代理配置示例。

### 新增文件

- `compose.production.external-caddy.yml`

### 修改文件

- `README.md`
- `backend/app/main.py`
- `frontend/package.json`
- `frontend/package-lock.json`
- `docs/08_部署说明.md`
- `docs/09_开发日志.md`
- `docs/11_服务器上线清单.md`

### 验收标准

- 外部 Caddy Compose 配置校验通过。
- 前端只绑定到 `127.0.0.1`，不直接暴露公网。

## v0.26.0-broker-style-holdings - 2026-07-10

阶段：当前持仓体验优化

### 完成内容

- 当前持仓表新增持仓名称、资产类型、现价、成本价、持仓盈亏和收益率字段。
- 新增 Alembic 迁移 `20260710_0002_position_broker_fields`。
- 后端保存持仓时支持用 `持仓数量 × 现价` 自动计算当前市值。
- 后端保存持仓时支持用 `持仓数量 × 成本价` 自动计算持仓成本、盈亏和收益率。
- 前端“当前持仓”页面重写为券商式录入：代码、名称、类型、持仓数量、现价、成本价。
- 前端新增账户汇总：总市值、总成本、持仓盈亏和收益率。
- 前端持仓列表新增名称、资产类型、格式化金额和格式化权重。

### 新增文件

- `backend/alembic/versions/20260710_0002_position_broker_fields.py`

### 修改文件

- `README.md`
- `backend/app/main.py`
- `backend/app/models/portfolio.py`
- `backend/app/schemas/portfolio_schema.py`
- `backend/app/services/holding_service.py`
- `backend/tests/test_holding_service.py`
- `frontend/package.json`
- `frontend/package-lock.json`
- `frontend/src/api/client.ts`
- `frontend/src/styles.css`
- `frontend/src/views/Holdings.vue`
- `docs/03_数据库设计.md`
- `docs/04_API接口文档.md`
- `docs/09_开发日志.md`

### 验收标准

- Alembic 能升级到最新迁移。
- 后端测试 `54 passed`。
- 前端生产构建通过。

## v0.27.0-auto-resolved-holdings - 2026-07-10

阶段：当前持仓自动补全

### 完成内容

- 当前持仓录入改为用户只填写代码、持仓数量和成本价。
- 后端保存持仓时会按代码从 `asset_master` 补全持仓名称和资产类型。
- 后端保存持仓时会按代码从 `market_data_clean` 获取最新收盘价作为现价。
- 新增 `/api/portfolio/positions/resolve`，用于前端保存前批量预览名称、类型、现价和价格日期。
- 前端“当前持仓”页面新增“自动补全”按钮，名称、类型和现价改为只读展示。
- 保存失败时前端会显示后端返回的中文原因，例如某代码缺少最新行情。
- 系统流程文档补充当前持仓录入规则，明确不连接券商账户、不自动操作资金。

### 修改文件

- `README.md`
- `backend/app/main.py`
- `backend/app/api/portfolio_api.py`
- `backend/app/schemas/portfolio_schema.py`
- `backend/app/services/holding_service.py`
- `backend/tests/test_holding_service.py`
- `frontend/package.json`
- `frontend/package-lock.json`
- `frontend/src/api/client.ts`
- `frontend/src/views/Holdings.vue`
- `docs/04_API接口文档.md`
- `docs/09_开发日志.md`
- `docs/10_系统流程说明.md`

### 验收标准

- 后端能在只提交 `symbol`、`quantity`、`cost_price` 时自动计算持仓市值和盈亏。
- 前端当前持仓页不再要求手动输入名称、类型和现价。
- 后端测试通过。
- 前端生产构建通过。

## v0.28.0-holdings-dialog-editor - 2026-07-10

阶段：当前持仓弹窗维护

### 完成内容

- 当前持仓主表格改为只读展示，避免在表格内直接输入导致列宽、滚动和误触问题。
- 新增“新增持仓”弹窗，用户只填写代码、持仓数量和成本价。
- 新增“编辑”弹窗，可修改已录入持仓的代码、数量和成本价。
- 新增“补仓”弹窗，输入追加数量和补仓成交价后，系统自动计算补仓后的总数量和加权成本价。
- “自动补全”调整为“刷新现价”，用于批量更新名称、类型、最新价格和价格日期。
- 空持仓列表展示引导文案，提示用户从“新增持仓”开始录入。

### 修改文件

- `README.md`
- `backend/app/main.py`
- `frontend/package.json`
- `frontend/package-lock.json`
- `frontend/src/views/Holdings.vue`
- `docs/01_项目说明.md`
- `docs/04_API接口文档.md`
- `docs/09_开发日志.md`

### 验收标准

- 前端当前持仓页新增、编辑、补仓均通过弹窗完成。
- 表格不再出现直接编辑控件。
- 前端生产构建通过。

## v0.29.0-etf-pool-screener - 2026-07-10

阶段：ETF 池筛选器增强

### 完成内容

- ETF 池页面增加总览指标：ETF 总数、启用研究、跨境 ETF、高风险主题。
- ETF 池页面增加快捷分类筛选：全部、宽基权益、债券、黄金/商品、货币现金、跨境 QDII。
- 筛选条件新增风险上限和跨境状态，便于只保留适合当前策略的 ETF 范围。
- ETF 主数据表改为更接近 screener 的展示方式，用中文标签展示资产类别、区域和风险等级。
- 精选 ETF 示例池从少量样例扩展到宽基、行业主题、港美 QDII、黄金、债券和货币 ETF。
- 全局样式补充 ETF 池筛选栏和移动端指标布局，减少页面挤压。

### 修改文件

- `README.md`
- `backend/app/main.py`
- `frontend/package.json`
- `frontend/package-lock.json`
- `frontend/src/views/Assets.vue`
- `frontend/src/styles.css`
- `docs/01_项目说明.md`
- `docs/04_API接口文档.md`
- `docs/09_开发日志.md`

### 验收标准

- ETF 池页面能按类别、区域、风险上限、跨境状态和启用状态筛选。
- 精选 ETF 示例池可一键导入。
- 前端生产构建通过。

## v0.29.1-etf-pool-route-fix - 2026-07-10

阶段：ETF 池路由兼容修复

### 完成内容

- 修复直接访问 `/assets/` 时被 nginx 静态资源规则拦截并返回 404 的问题。
- 保持 `/assets/*.js`、`/assets/*.css` 等 Vite 静态资源继续优先按真实文件返回。

### 修改文件

- `README.md`
- `backend/app/main.py`
- `frontend/package.json`
- `frontend/package-lock.json`
- `frontend/nginx.conf`
- `docs/01_项目说明.md`
- `docs/04_API接口文档.md`
- `docs/09_开发日志.md`

### 验收标准

- `/assets` 和 `/assets/` 均能进入前端 ETF 池页面。
- 前端静态资源仍能正常加载。

## v0.29.2-etf-pool-route-fix - 2026-07-10

阶段：ETF 池路由精确匹配修复

### 完成内容

- 新增 nginx 精确匹配 `location = /assets`，避免 `/assets` 被静态资源目录重定向到 `http://.../assets/`。
- 保留 `/assets/` 对前端路由的回退支持，兼容用户复制带尾斜杠的地址。

### 修改文件

- `README.md`
- `backend/app/main.py`
- `frontend/package.json`
- `frontend/package-lock.json`
- `frontend/nginx.conf`
- `docs/01_项目说明.md`
- `docs/04_API接口文档.md`
- `docs/09_开发日志.md`

### 验收标准

- `/assets` 返回 200，不再跳转到 `http://.../assets/`。
- `/assets/` 返回 200。
- 前端静态资源正常加载。

## v0.30.0-portfolio-xray-dca-goals - 2026-07-11

阶段：组合风险透视与目标定投

### 完成内容

- 新增 `GET /api/portfolio/xray`，返回组合暴露和策略前检查结果。
- 组合风险透视按资产类别、区域和风险等级对比当前持仓权重与目标组合权重。
- 策略前检查展示启用 ETF 数、持仓数、目标组合数、最新行情日期、最新因子日期、缺行情数量、高风险目标权重、跨境目标权重和单只最高目标权重。
- 定投计划新增 `target_annual_return` 和 `investment_mode` 字段。
- 定投计划创建支持填写 `total_budget`，系统按投入期数自动换算每期金额。
- 前端“定投计划”页面新增准备资金、目标年化和执行模式。
- 前端“组合工作台”新增策略前检查和组合风险透视 X-Ray 面板。
- 文档明确：目标年化只是策略参数，不代表收益承诺；当前不自动下单，未来自动买卖需独立执行层和审计风控。
- 新增 ETF 功能优化路线图，记录 ETF 对比、可交易性评分、回测增强、报告增强和未来自动执行层规划。

### 新增文件

- `backend/alembic/versions/20260711_0003_investment_plan_target_fields.py`
- `backend/app/services/portfolio_xray_service.py`
- `backend/tests/test_portfolio_xray_service.py`
- `docs/12_ETF功能优化路线图.md`

### 修改文件

- `README.md`
- `backend/app/api/portfolio_api.py`
- `backend/app/main.py`
- `backend/app/models/portfolio.py`
- `backend/app/schemas/portfolio_schema.py`
- `backend/app/services/investment_plan_service.py`
- `backend/tests/test_investment_plan_service.py`
- `frontend/package.json`
- `frontend/package-lock.json`
- `frontend/src/api/client.ts`
- `frontend/src/views/InvestmentPlans.vue`
- `frontend/src/views/PortfolioWorkbench.vue`
- `docs/01_项目说明.md`
- `docs/03_数据库设计.md`
- `docs/04_API接口文档.md`
- `docs/09_开发日志.md`
- `docs/10_系统流程说明.md`

### 验收标准

- Alembic 能升级到最新迁移。
- 定投计划可按总预算自动计算每期金额。
- 组合工作台可展示 X-Ray 和策略前检查。
- 后端测试通过。
- 前端生产构建通过。

## v0.41.0-etf-profile-fields - 2026-07-13

阶段：ETF 主数据增强与 ETF 池页面中文修复

### 完成内容

- `asset_master` 新增 ETF 主资料字段：基金公司、跟踪指数、上市日期、基金规模、管理费、托管费、综合费率、跟踪误差、最新溢价率。
- 新增 Alembic 迁移 `20260713_0005_asset_master_etf_profile.py`。
- `AssetRead`、`AssetUpsertItem`、`AssetUpdateRequest` 支持新增字段。
- ETF 池页面重写为正常中文，新增资料完整度、基金公司、跟踪指数、规模、费率展示。
- ETF 池页面新增“编辑资料”弹窗，可手动维护 ETF 主资料。
- ETF 详情页重写为正常中文，并展示主资料、费率、规模、跟踪误差和溢价率。
- 修复 `asset_service.py`、`asset_api.py` 和资产服务测试中的乱码文案。

### 设计说明

- 本版先完成字段、保存、展示和手动维护能力。
- 外部数据源自动补全基金公司、规模、费率、跟踪误差等信息留到下一版继续做。
- 全市场 ETF 同步仍保留已有启用状态和人工维护的资料字段，避免同步时覆盖用户手动补充的数据。

### 验收要点

- ETF 池可以显示资料完整度和新增主资料字段。
- 点击“编辑”可以保存基金公司、跟踪指数、规模、费率等资料。
- ETF 详情页可以看到新增主资料。
- 数据库迁移能在服务器部署时自动执行。

## v0.40.0-docs-refresh - 2026-07-13

阶段：核心文档全量修复与 AI 接手上下文整理

### 完成内容

- 重写 `README.md`，清理历史乱码，补充当前版本、线上地址、技术栈、启动方式、部署方式和安全边界。
- 重写 `docs/00_文档导航.md`，形成给自己和 AI 接手项目的最小上下文清单。
- 重写 `docs/01_项目说明.md`，明确项目定位、当前能力、未完成能力、投资边界和数据源边界。
- 重写 `docs/02_系统架构.md`，梳理前后端分层、服务模块、任务执行、鉴权和部署结构。
- 重写 `docs/03_数据库设计.md`，补齐核心表说明和 `agent_analysis_log`。
- 重写 `docs/04_API接口文档.md`，按业务模块整理当前常用 API。
- 重写 `docs/05_策略设计.md`、`docs/06_风控设计.md`、`docs/07_回测设计.md`。
- 重写 `docs/08_部署说明.md`、`docs/10_系统流程说明.md`、`docs/11_服务器上线清单.md`、`docs/12_ETF功能优化路线图.md`。

### 设计说明

- 本版不改变业务代码和数据库结构，只修复维护文档。
- 文档以“未来交给 AI 快速接手”为目标，优先保证当前功能、边界、路径和部署命令准确。
- 历史开发日志保留在本文件后续内容中，新的干净版本记录放在顶部。

### 验收要点

- README 和核心 docs 应为正常中文。
- 新维护者可通过文档快速理解项目结构、功能边界、部署方式和下一步路线。
- 前端构建、后端测试和部署流程不应受影响。

## v0.39.0-agent-analysis-history - 2026-07-13

阶段：AI 投研落库、自动补全与前端可用性增强

### 完成内容

- 新增 `agent_analysis_log` 数据表和 Alembic 迁移 `20260713_0004_agent_analysis_log.py`，每次运行 ETF AI 投研后自动保存分析结果。
- `POST /api/agent-analysis/etf` 新增 `auto_sync` 参数，默认在分析前尝试补全该 ETF 的资产主数据和最近行情。
- 新增 `GET /api/agent-analysis/etf/history`，支持按 ETF 代码查询最近 AI 投研记录。
- 前端“AI 投研委员会”页面新增“自动补全数据”开关和“最近分析记录”表格，可回看历史结论。
- 修复 AI 投研页面和持仓服务中的乱码文案，避免继续出现不可读提示。
- 持仓服务继续保持“代码唯一、名称/类型/现价由系统补全，用户主要填写数量和成本价”的录入逻辑。

### 设计说明

- AI 投研历史只保存分析结果、Agent 证据、风险提示和最终结论，不保存 DeepSeek API Key。
- 自动补全优先使用本地已有行情；缺失时同步最近 45 天行情，仍缺失时尝试 Eastmoney 单只 ETF 报价兜底。
- DeepSeek 仍只负责结论润色和综合解释，基础判断来自本地行情、因子、持仓和目标组合数据。
- 当前系统仍不自动下单，所有建议都需要用户人工确认。

### 验收要点

- 运行一次 AI 投研后，页面“最近分析记录”应出现该记录。
- 输入本地未登记但真实存在的 ETF 代码时，开启“自动补全数据”后应先尝试补齐基础信息和行情。
- 后端测试、前端构建、Docker Compose 配置检查和生产部署均应通过。
